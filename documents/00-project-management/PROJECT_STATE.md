# Project State Baseline

**Baseline Date:** 2026-06-19 (last refreshed 2026-07-16 after R05b1 implementation + corrective pass on `dev`; planning-only R14a closed/accepted 2026-07-16; frontend-only R05b1 implemented on `dev` 2026-07-16 with corrective pass 2026-07-16 pending Product Owner acceptance)
**Branch:** dev
**Original Sprint 001 Baseline Commit:** `0131b44ff1ac6b263cebef6d2526196042c5560f`
**Sprint 003 Implementation Predecessor Commit:** `9c0fba72ca21f933c88817c3b4975bf3319f9b2b`
**Sprint 003 Final Reviewed Commit:** `b0a822478c5d4cee6162702d5969605cfc2702f2`

## Current State (as of 2026-07-16)

**Active sprint:** **R05b1 — Room entry, creation, and invite redemption UI**, implemented on `dev` (2026-07-16) with a corrective pass (2026-07-16) per Architect review; pending Product Owner acceptance. R05b1 is the first narrow slice of the legacy R05b bucket. R05b1 is **frontend-only**: it adds a new authenticated route `/rooms` (router name `RoomEntry`), a focused `frontend/src/views/RoomEntryView.vue`, four thin frontend API methods (`api.createRoom` / `api.listRooms` / `api.getRoom` / `api.redeemInvite`), a `Rooms` navigation control on the global `Dashboard`, and changes the `RoomView` `Back` to navigate to `RoomEntry` (was `Dashboard`). R05b1 does NOT add `api.joinRoom` (joining a room means invite redemption; the backend never exposed a `/api/rooms/{slug}/join` route); R05b1 does NOT introduce player-lease claim/read/heartbeat/release UI (R05b2). R05b1 preserves the global dashboard as the login destination until R14d. R05b1 preserves the global `/ws` 16-event inventory, the R07b/R07d/R09a/R09b/R09c/R09d/R09f/R10b/R11a per-room WebSocket events, the R06 `room_archived` wire value + payload, the R05 in-memory session-token model, the R10a members-list response wrapper, the schema version 8, and all prior sprint runtime surfaces. **Corrective-pass invariants (2026-07-16):** the manual-open form short-circuits with an immediate `return` after surfacing the "This room is no longer available." message for archived (or otherwise non-active) rooms — those rooms MUST NOT navigate; the user remains on `RoomEntry` and can still join through an invite-token redemption; the raw (unencoded) invite token MUST NOT appear in the SPA/router URL, route query/history, `localStorage` / `sessionStorage` (every key/value), `console.log` / `console.warn` / `console.error`, toast messages, the rendered DOM text, or any router `push` argument (the encoded backend API URL is the documented wire shape verified separately by `frontend/src/services/__tests__/roomApi.spec.js`); a new `frontend/src/router/__tests__/router.spec.js` pins the production router module against the documented guard contract. Verification (2026-07-16): `cd frontend && npm run test:unit -- --run` (352/352 pass — 345 baseline + 7 new production-router-guard tests), `cd frontend && npm run build` (clean), `git diff --check` (clean). R05b remains incomplete as an R14c prerequisite until both R05b1 AND R05b2 are accepted by the Product Owner. R13 / R10f+ / R11b+ / R12 remain out of scope. R05b2 / R09h / R09i / R14b / R14c / R14d / R14e remain planned and NOT marked active. See `SPRINTS/023-room-entry-ui.md` for the full R05b1 contract + the R05b1/R05b2 split + the deferred R05b2 scope + the corrective-pass changes.

**R14a — Legacy global state migration and contract retirement plan** was the most recent planning/design sprint on `dev`; **closed on `dev` (2026-07-15) and accepted by the Product Owner on 2026-07-16** (revised six times after Architect reviews per the corrective Builder prompts on Issue #20). R14a was **documentation only**. R14a narrows + precedes the legacy R14 stub (`019-global-contract-cleanup-and-documentation.md`). R14a does NOT change runtime, dependencies, REST routes, WebSocket event names/payloads, store mutators, migrations, Docker / Nginx / deployment, or auth / session behaviour. The schema-version plan is **v8 → v9 in R14b** (new migration `0009_room_cutover_support.up.sql` creates BOTH `room_activities` AND `room_cutover_marker` — the CLI does NOT create schema ad hoc) **→ v10 in R14e** (new forward migration `0010_drop_legacy_global_tables.up.sql` whose matching `.down.sql` recreates ONLY the four dropped tables — `queue_state` (with `data TEXT NOT NULL` and no seedable NULL), `activities` (with its OWN `activities_id_seq`), `auto_queue_config` (with the singleton seed row at its initial values `(1, FALSE, 'related')`), `play_history` (with its OWN `play_history_id_seq` and the `idx_play_history_played_at` index) — and MUST NOT recreate any account-table objects (`users`, `user_sessions`, `priority_transactions`, their sequences, unique constraints, FK constraints); the documented default is that **dropped data cannot be restored**, and the reverse-copy strategy relies on the **retained `pg_dump`** (≥30 days) or a separately approved reverse-copy mechanism); historical migration files `0001`..`0008` remain on disk byte-for-byte and `0001_initial.up.sql` is NEVER rewritten. R14a does NOT pull forward R13 (session / authentication / authorization hardening), R10f+ (deferred room lifecycle hardening), R11b+ (deferred R11 chat slice), or R12 (search and room discovery). Sprint execution order is fixed at **`R05b → R09h → R14b → R09i → R14d → R14c → R14e`**; R09i follows R14b because R14b creates `room_activities` that R09i wires. **`--room-cutover-authoritative` is a runtime server startup flag** (Go runtime arg parsed at `main()` startup, NOT a build-time-only constant); the flag is **owned by R14c** — R14b does NOT introduce it, does NOT read it, and does NOT switch activity-writer or route registration based on it; R14b's binary is always pre-cutover. R14c composition reads the flag and injects either a no-op activity writer (when `false`) or the real `room_activities`-writing implementation (when `true`); starting the server with `true` fails closed unless schema version ≥ 9 AND the `room_cutover_marker` row exists (durable activation proof). The Go backend NEVER consumes the SPA flag `VITE_ROOM_CUTOVER_AUTHORITATIVE`. **The R14c maintenance window keeps public traffic closed until both the `true` server and the `true` SPA bundle are deployed as a paired set and smoke-checked; mid-window rollback restores the `false` server and `false` SPA bundle as one pair before reopening traffic.** The **marker stores `target_room_id` and `host_user_id` as immutable integer audit snapshots WITHOUT foreign keys** (migration evidence MUST NOT own room or user lifecycle); the **marker timestamp field `cutover_pre_commit_at` is supplied explicitly by the CLI** at marker insertion (NOT `DEFAULT NOW()`). R14d's behavioral activation is deferred to the R14c maintenance window behind the **single named gate `VITE_ROOM_CUTOVER_AUTHORITATIVE`** (Vite build-time; two bundle variants — `false` pre-R14c / `true` post-R14c; matches `--room-cutover-authoritative`; NO runtime feature flag). **R09i write behavior must remain DISABLED until R14c has copied legacy activities and resynchronized `room_activities_id_seq`** (activity-ID collision guard). R05b (Room entry / player-lease UI), R09h (Room vote-to-prioritize parity — blocks the entire R14c cutover), R09i (Room activity runtime parity — BACKEND-ONLY; blocks R14c unless waived; writes disabled until R14c), R14b (room-cutover mechanism — dedicated `cmd/room-cutover` CLI with NEW dedicated `room_cutover_marker` — marker has audit-snapshot integers, no FKs; no `abort` subcommand; deferred `pg_advisory_unlock`; marker inserted in same transaction as room + host + copy; marker timestamp `cutover_pre_commit_at` supplied by CLI; **does NOT introduce the runtime server startup flag and does NOT select activity-writer implementations — those belong to R14c**; lands `0009` migration), R14c (coordinated production cutover; ALL listed legacy routes retire atomically with `410 Gone` tombstone handlers; `/ws` upgrade rejected in HTTP phase), R14d (frontend global-path removal — implementation accepted pre-R14c; behavior activates in R14c via `VITE_ROOM_CUTOVER_AUTHORITATIVE=true`), and R14e (schema cleanup — drops legacy global tables only via `0010`; `room_play_history` is NOT touched) are planned and are NOT marked active. The new ADR `documents/00-project-management/ADRS/003-legacy-global-state-migration-and-contract-retirement.md` is **Accepted** (Product Owner 2026-07-16). See [`SPRINTS/022-legacy-global-state-migration-and-contract-retirement-plan.md`](./SPRINTS/022-legacy-global-state-migration-and-contract-retirement-plan.md) for the full R14a contract. R11a — Room chat contract and minimal vertical slice — was **closed on `dev` (2026-07-15)** and **accepted by the Product Owner on 2026-07-15**; R11b+ remains planned and is NOT marked active. R12, R13, and the legacy R14 stub remain planned and are NOT marked active.

**Accepted state on `dev`:** Room lifecycle (create / archive), membership, invites, queue, playback controls (play/pause/sync/skip/ended/prev/volume), voting (room-scoped vote-to-skip), per-room auto-queue (config + history + WebSocket deltas), room archive (host-driven) and member removal (host-driven), and the frontend wiring for all of the above are largely implemented and accepted by the Product Owner. The R10 closure (R10a contract + R10b backend + R10c frontend + R10d polish) is closed and accepted. R10e (docs + tooling refresh after R10d acceptance) is also closed and accepted (2026-07-10). The first narrow vertical slice of the R11 room-chat feature (R11a) was implemented on `dev` (2026-07-14), went through a corrective pass (2026-07-14), a final corrective pass (2026-07-14), and a deferred lifecycle follow-up (2026-07-15), and is closed and accepted by the Product Owner (2026-07-15). R11b+ remains planned and is NOT pulled forward; R10f+ remains planned and is NOT pulled forward; R12/R13/R14 remain planned and are NOT pulled forward. The current active sprint is R14a (see Current State above); no runtime / contract sprint is currently active beyond R14a's planning-only contract design.

**Room-feature completion estimate:** ~80–85% accepted (counting the deferred room lifecycle hardening below as core room scope). With R11a now accepted, room features stay in the ~80–85% range since R11a is a brand-new feature area rather than additional room-lifecycle surface.

**Still planned (NOT active):** R11b+ — richer room chat (message edit/delete, moderation, audit, retention, attachments, typing indicators, read receipts, dedicated chat WS, etc.); R12 — Search and room discovery; R13 — Authentication and authorization hardening; R14 — Global contract cleanup and documentation.

**Deferred, NOT required before R11b+:** R10f+ — Deferred room lifecycle hardening (hard delete / retention windows, admin audit read surface, per-room session revocation, archived-room cleanup). The Product Owner has not asked for this before R11b+; if it is not pulled forward, it remains a post-R10 / post-R11 bucket.

## Current Sprint

**R14a — Legacy global state migration and contract retirement plan** was the **most recent planning/design sprint on `dev` (2026-07-15)** and was **documentation only** (revised six times after Architect reviews; first-pass through sixth-pass corrections applied 2026-07-15 per the corrective Builder prompts on Issue #20). **R14a was closed on `dev` (2026-07-15) and accepted by the Product Owner on 2026-07-16.** R14a does NOT change runtime, dependencies, REST routes, WebSocket event names/payloads, store mutators, migrations, Docker / Nginx / deployment, or auth / session behaviour. The schema-version plan is **v8 → v9 in R14b** (new migration `0009_room_cutover_support.up.sql` creates BOTH `room_activities` AND `room_cutover_marker`) **→ v10 in R14e** (new forward migration `0010_drop_legacy_global_tables.up.sql` whose matching `.down.sql` MUST restore exact legacy schema structure — columns, indexes, sequences, constraints, defaults, `auto_queue_config` singleton seed at id=1 — and CANNOT restore dropped data; documented in the file header); historical migration files `0001`..`0008` remain on disk byte-for-byte and `0001_initial.up.sql` is NEVER rewritten. The R14a contract (see [`SPRINTS/022-legacy-global-state-migration-and-contract-retirement-plan.md`](./SPRINTS/022-legacy-global-state-migration-and-contract-retirement-plan.md)) settles: a new `room_activities` table + dedicated repo (NOT coupled to `QueueRepository`) with live runtime write parity in a separate R09i (BACKEND-ONLY) sprint; host bootstrap via the canonical PostgreSQL `users.id` (`--host-user-id=<positive integer>`; PK lookup; no inference from joiners / roles / emails / lease / frontend); the one-host invariant is **per room**, not per user globally; `users` / `user_sessions` / `priority_transactions` are **NOT recopied** at cutover (account-scoped per ADR 001 §3 Decision 9); lossless source-to-target mapping — the `queue_state` → `room_queue_state` conversion uses the default `encoding/json` tagged-field round-trip on `entity.Queue` (no custom `MarshalJSON` / `UnmarshalJSON` methods are assumed), validating all invariants and computing SHA256 over the canonical JSONB bytes; preserved id/timestamp/type/user/description/count for `activities`; `auto_queue_config` keyed by `room_id`; `play_history` copy to `room_play_history` handles id collisions via `legacy_id_offset = COALESCE(MAX(room_play_history.id), 0)` recorded in the marker; the room-cutover mechanism is a **NEW dedicated `cmd/room-cutover` CLI with NEW dedicated `room_cutover_marker` contract** — R03's `cmd/migrate-data` + `migration_marker` are preserved untouched and are NOT reused; `cmd/room-cutover` ships `plan` / `up` / `verify` subcommands (**NO `abort`**) because PostgreSQL session locks belong to the connection that acquired them; the lock contract uses **deferred `pg_advisory_unlock(987654321)` on the same pinned connection** (mirroring R03's idiom) with connection close / `SIGTERM` as the fallback; the **marker stores `target_room_id` and `host_user_id` as immutable integer audit snapshots WITHOUT foreign keys** (migration evidence MUST NOT own room or user lifecycle); the **marker is inserted in the same transaction** as the room, host membership, and copied state — no post-commit marker write; the **marker timestamp field `cutover_pre_commit_at` is supplied explicitly by the CLI at marker insertion** (NOT `DEFAULT NOW()`); mid-flight crash leaves NO partial state; R14b builds + verifies the offline mechanism (schema v8 → v9); R14b does **NOT** introduce or read the runtime server startup flag `--room-cutover-authoritative` — that flag is owned by R14c, which introduces it and uses it to select between the no-op and real `RoomActivityRepository` at composition time; **R09i follows R14b** because R14b creates `room_activities` and R09i wires the runtime write path; **R09i composes the no-op `RoomActivityRepository` unconditionally in every pre-R14c deployment** — a Vue build-time constant cannot control backend dependency injection; the no-op writer is replaced by the real `room_activities`-writing implementation only after the R14c maintenance window flips the server runtime flag (and only if the startup guard passes); **R09i write behavior must remain DISABLED until R14c has copied legacy activities and resynchronized `room_activities_id_seq`** (activity-ID collision guard, symmetric with the `legacy_id_offset` mechanism for `room_play_history`); the Go backend NEVER consumes the SPA flag `VITE_ROOM_CUTOVER_AUTHORITATIVE`; the **frontend cutover-state gate is a single named mechanism `VITE_ROOM_CUTOVER_AUTHORITATIVE`** (Vite build-time; two bundle variants — `false` pre-R14c / `true` post-R14c; matches `--room-cutover-authoritative`; NO runtime feature flag); **R14d's behavioral activation is deferred to the R14c maintenance window** behind that gate (implementation may be accepted before R14c but the SPA continues exposing the legacy dashboard pre-cutover so the still-authoritative global queue remains reachable); **R14c owns the coordinated production execution** with a binary started with `--room-cutover-authoritative=true` that registers **repository-free tombstone handlers returning `410 Gone`** with envelope `{error, code: "global_contract_retired", documentation}` plus HTTP `Link: </api/rooms>; rel="successor-version"` (NOT `404` unregistration); the global `/ws` upgrade is **rejected with `410` in the HTTP phase**; **R14c is atomic across ALL listed legacy routes** — no partial cutover; **three blocking prerequisites** for R14c: **R05b** (room entry / player-lease UI), **R09h** (vote-to-prioritize parity — blocks the entire R14c cutover, not just `/api/vote/prioritize` retirement), and **R09i** (room-activity runtime parity — BACKEND-ONLY; wires room mutations to `room_activities`; writes disabled until R14c; blocks R14c unless the Product Owner explicitly waives, recorded on Issue #20); rollback target is the **immediately pre-cutover production release** with `--room-cutover-authoritative=false` (NOT an "R03 binary" — R03 is the unrelated SQLite-to-PostgreSQL migration release); impossible rollback / client-continuity claims are replaced by a **deployment compatibility matrix**; the future sprint sequence is **R05b → R09h → R14b → R09i → R14d → R14c → R14e**. **Every new migration ships a matching `.down.sql`**; `0010_drop_legacy_global_tables.down.sql` recreates ONLY the four dropped tables (`queue_state` with `data TEXT NOT NULL` and no seedable NULL; `activities` with its OWN `activities_id_seq`; `auto_queue_config` with the singleton seed row at its initial values `(1, FALSE, 'related')`; `play_history` with its OWN `play_history_id_seq` and the `idx_play_history_played_at` index); it MUST NOT recreate any account-table objects (`users`, `user_sessions`, `priority_transactions`, their sequences, unique constraints, FK constraints) — those tables survive `0010.up.sql`; the documented default is that **dropped data cannot be restored** and the reverse-copy strategy relies on the **retained `pg_dump`** (≥30 days) or a separately approved reverse-copy mechanism (this is documented in the file header). The new ADR `documents/00-project-management/ADRS/003-legacy-global-state-migration-and-contract-retirement.md` is **Accepted** (Product Owner 2026-07-16); ADR 001 body §3–§18 is unchanged byte-for-byte except for one metadata supersession note near the header; ADR 002 body is unchanged. R13 (session / authentication / authorization hardening), R10f+ (deferred room lifecycle hardening), R11b+ (deferred R11 chat slice), and R12 (search and room discovery) are NOT pulled into R14a scope. **With R14a closed/accepted, no sprint is currently active on `dev`; the next sprint to shape is per the room epic sequence below, at the Product Owner's discretion.** R05b, R09h, R09i, R14b, R14c, R14d, and R14e are planned and are NOT marked active.

The most recently closed and accepted sprint is **Sprint R11a — Room chat contract and minimal vertical slice** (closed 2026-07-15, accepted by the Product Owner 2026-07-15). R11a is the first narrow, full-stack vertical slice of the legacy R11 room-chat bucket: backend (entity, repository, use case, HTTP handlers, additive per-room WebSocket envelope) + frontend (api methods, store mutators, minimal `RoomView` chat panel) + a new PostgreSQL migration (`0008_room_chat_messages`). Active room members can POST a plain-text message and GET recent messages; new persisted messages broadcast over the existing per-room WebSocket stream as an additive `room_chat_message_created` envelope. Sender identity comes from the bearer/session actor (never the request body); room identity comes from the path slug (never the request body). Validation: trim leading/trailing whitespace, normalize CRLF/CR to LF, reject empty content after trim, max 500 Unicode code points (`utf8.RuneCountInString` server-side; `Array.from(str).length` client-side), plain text only (no HTML/rich-text processing). The wire NEVER exposes the sender email; the `display_name` falls back to `user #<id>` when the user row's `display_name` is empty. R11a does NOT introduce a retention purge job, moderation surface, audit log, attachments, markdown/rich text, typing indicators, read receipts, WebSocket send command, dedicated chat WebSocket endpoint, or cross-process guarantees. R11a preserves: the global `/ws` 16-event inventory, the R06 `room_archived` wire value + payload, the R07b/R07d/R09a/R09b/R09c/R09d/R09f/R10b per-room WebSocket events, the R05 in-memory session-token model, the R10a members-list response wrapper, the schema version (now 8 after migration 0008), the global room-queue + auto-queue + vote + playback + member-removal surfaces, the R10f+ deferred lifecycle hardening bucket, and the R12/R13/R14 planned sprints. R10e (the previous active sprint — docs + tooling refresh after R10d acceptance) is closed and accepted by the Product Owner (2026-07-10). R11a touches only the documented R11a files; it does NOT change dependency versions, REST routes unrelated to chat, WebSocket event names/payloads unrelated to chat, store mutators unrelated to chat, runtime code unrelated to chat, Docker/Nginx/deployment, auth/session behavior, or R10f+. Frontend history seed is fired AFTER the first per-room `room_queue_sync` event (the per-room hub's confirmation that the client is registered) and runs at most once per WS connection; the input has no native `maxlength` attribute and uses `canEditChat` / `canSendChat` so the user can shorten an over-limit draft. Final frontend test count: 305 tests across the frontend suite, all green; `git diff --check` clean; `cd frontend && npm run build` clean. Verification: `go test -count=1 ./internal/usecase/roomchat/...`, `go test -count=1 ./internal/delivery/http/...`, `go test -count=1 ./internal/delivery/ws/...`, `go test -count=1 ./internal/infrastructure/persistence/...`, `go test -count=1 ./cmd/server/...`, `go test -race -count=1 ./internal/delivery/ws/... ./internal/usecase/roomchat/...`, `cd frontend && npm run test:unit -- --run` (305/305), `cd frontend && npm run build`, `git diff --check`. R11a is now closed and accepted; R11b+ (deferred R11 surface — moderation, edit/delete, audit, retention, attachments, typing indicators, read receipts, WebSocket send command, dedicated chat WS, richer sender profile) remains planned and is NOT pulled forward.

The most recently closed sprint that introduced architectural changes documented here is **Sprint 012 / R07d — Room-scoped playback queue (one room-scoped mutation: `POST /api/rooms/{slug}/queue/prioritize`, plus the matching `room_queue_song_prioritized` per-room WebSocket delta and a minimal RoomView Prioritize control)** (closed 2026-06-30, accepted by the Product Owner). R07d closed the same R07 narrow slice pattern as R07a/R07b/R07c: one additive room-scoped mutation, one matching per-room WebSocket delta, and a minimal frontend control, with no changes to global queue behavior, global `/ws` contract, auth, Docker, or deployment. The closure pass tightened three narrow invariants on `dev`: the handler request shape uses `*int` so a missing `song_index` returns 400; the interactor returns the post-mutation song so the broadcast payload carries `is_prioritized: true`; and the frontend store fallback reflects the snake-case `is_prioritized` field. The most recently closed sprint that introduced architectural changes documented here is **Sprint 015 / R09b — Room vote-to-skip backend contract** (closed 2026-06-30, accepted by the Product Owner). R09b added one backend-only HTTP slice (`POST /api/rooms/{slug}/vote/skip`, behind `roomAuth`), two additive per-room WebSocket events (`room_vote_updated`, `room_vote_resolved`), and an additive reuse of the existing `room_playback_song_advanced` event with `reason="skip"` on a passed vote. Vote sessions are in-memory, room-scoped, current-song-scoped, 30-second expiry, single-instance only, never persisted, with a strict-majority threshold `max(2, n/2 + 1)` (n = unique connected `userID`s in the room) captured at session creation. R09b does NOT change the global `/api/vote/...`, global `/api/queue/...`, global `/ws` 16-event inventory, priority balances, auto-queue, Docker, CORS, auth/session design, or any frontend UI; the previously accepted sprint is Sprint 014 / R09a (lease-aware room playback controls, accepted 2026-06-30). With R09b's closure there is no currently active sprint on `dev`; the next sprint to shape is per the room epic sequence below. **Sprint 016 / R09c — Room-scoped volume command** is implemented on `dev` (2026-06-30) and accepted by the Product Owner (2026-07-01). R09c added one backend-only HTTP slice (`POST /api/rooms/{slug}/playback/volume`, behind `roomAuth`), one additive per-room WebSocket event (`room_playback_volume_changed`), and a narrow frontend wiring (one API method + one WS listener + a small Vol± control surface gated on the existing lease-holder UI affordance). The volume command is intentionally NOT persisted: there is no `entity.Queue.Volume` field, no `room_queue_state` schema change, and the command does not call `queueRepo.Save`. The global `/api/queue/volume` contract, global `/ws` 16-event inventory, voting, priority balances, auto-queue, Docker, CORS, and auth/session design are all unchanged. The closure pass tightened two narrow UX details on `dev`: the frontend toast on `room_playback_volume_changed` now uses an explicit up/down branch (a non-up/down payload reads as a neutral "Volume changed." rather than silently defaulting to "decreased"); and the WS hub test file ends with a trailing newline matching the rest of the suite. With R09c's closure there is no currently active sprint on `dev`; the next sprint to shape is per the room epic sequence below. **Sprint R09d — Room-scoped previous playback command** is implemented on `dev` (2026-07-01) and accepted by the Product Owner (2026-07-01). R09d adds one lease-holder-only HTTP slice (`POST /api/rooms/{slug}/playback/prev`, behind `roomAuth`, empty body), one additive per-room WebSocket event (`room_playback_song_previous`), and a minimal frontend Prev control surface gated on the existing lease-holder UI affordance AND on `current_index > 0`. The command requires a valid current song and `CurrentIndex > 0`; on success it decrements `CurrentIndex`, sets `Elapsed = 0`, sets `Status = playing`, and persists the room queue. On no-current-song or already-first-song the command returns `400` with no mutation, no save, and no broadcast. A new `entity.Queue.PrevToPrevious` helper mirrors `AdvanceToNext`'s no-partial-mutation invariant and a local `roomqueue.ErrNoPreviousSong` sentinel is added so the handler maps cleanly to 400 without string-matching the legacy `Queue.Prev()` error. Auto-queue is NOT triggered. The global `/api/queue/prev` contract, global `/ws` 16-event inventory, voting, priority balances, Docker, CORS, and auth/session design remain untouched. With R09d's closure there is no currently active sprint on `dev`; the next sprint to shape is per the room epic sequence below. Remaining R09 scope (full media-player device integration) remains split into future R09f+ slices and deferred. **Sprint R09e — Room auto-queue contract design (planning/design only)** is closed on `dev` (2026-07-01) and accepted by the Product Owner (2026-07-01). R09e is a documentation-only planning/design sprint that shapes a narrow, reviewable contract for a future room auto-queue implementation: room-scoped (not silently controlled by the global auto-queue config), per-room config + history persistence, reuse of the existing `domain.RelatedSongFetcher`, a future `roomautoqueue.Interactor` orchestrator that uses a coordinator `mu` mutex guarding a per-room `inFlight` map keyed by room id/slug (NOT a single global `triggering` flag) and that never holds a lock during the slow `FetchRelated`, queue-owned conditional insertion + stale revalidation in `roomqueue.Interactor`, a per-room `room_auto_queue_added` event on `/ws/rooms/{slug}` only, host/admin toggle authorization, single-instance only, and zero-side-effect stale-candidate handling. R09e introduces no Go runtime code, no Vue runtime code, no migrations, no routes, no WebSocket constants, and no config changes. The global `/api/autoqueue/...` contract, the global `/ws` 16-event inventory, the `auto_queue_config` / `play_history` tables, the `domain.RelatedSongFetcher` / `domain.AutoQueueRepository` / `usecase/autoqueue.Interactor` / `PostgresAutoQueueRepository` / `YtDlpRelatedFetcher` are all unchanged. The future R09e+ runtime implementation is the next slice and remains deferred. See `documents/00-project-management/SPRINTS/021-room-auto-queue-contract-design.md`. **Sprint R09f — Room auto-queue backend runtime** is closed on `dev` (2026-07-01) and accepted by the Product Owner (2026-07-01). R09f is a backend-only runtime implementation that conforms to the R09e contract: two REST endpoints behind `roomAuth` (`GET /api/rooms/{slug}/autoqueue/status` for any active member; `POST /api/rooms/{slug}/autoqueue/toggle` for host/admin only — both with use-case-enforced authorization matching the roomqueue pattern); two per-room WebSocket events (`room_auto_queue_added`, `room_auto_queue_config_changed`) on `/ws/rooms/{slug}` only; a new `internal/usecase/roomautoqueue` package; per-room auto-queue persistence via migration `0007_room_auto_queue`; a queue-owned conditional insertion seam `roomqueue.Interactor.AddRoomAutoQueueSong` with `roomqueue.ErrRoomAutoQueueStale` zero-side-effect handling; a non-blocking `CheckAndTrigger` wired into `PlaybackEnded` / `SkipPlayback` / `SkipVote` / `RemoveSong` / `ClearQueue`; a per-room `inFlight` map guarded by a coordinator mutex whose critical section is narrow enough to preserve cross-room independence (the slow FetchRelated call is held outside any lock); a typed narrow broadcaster seam (production wiring connects the use case to `*ws.RoomWSHub.BroadcastRoomAutoQueueAdded` directly with the full R09e payload tuple — room_slug, song, source_song_title, current_index, current_song, status, elapsed, state); and `SetEnabled` serialized with the post-fetch insertion so a disable landing before the insertion drops the candidate with no queue save, no history append, and no broadcast. **Closure pass (2026-07-01, on `dev`):** tightened the `SetEnabled` ↔ `CheckAndTrigger` serialization invariant — `SetEnabled`'s `SaveConfig` write now runs under the same coordinator `mu` that the post-fetch recheck + `AddRoomAutoQueueSong` insertion hold, so a `SetEnabled(false)` save can no longer interleave between the final "still enabled?" check and the queue mutation (the prior code resolved room + checked role + called `GetConfig`/`SaveConfig` without ever taking the coordinator mutex). `GetConfig` (and all other slow paths in both methods) remains outside the mutex; the critical section is the minimum needed to serialize the disable write with the post-fetch decision window. The package-level `Locking discipline` comment and the `Interactor.mu` doc reflect the new serialization surface. The disabled-mid-flight test was replaced with two tests that pin the locked-correct outcomes directly — `TestSetEnabled_SerializedWithPostFetchCriticalSection` (asserts `SaveConfig` does not enter while the add fn holds mu, then proceeds after release) and `TestSetEnabled_RunsBeforePostFetchRecheck_DropsCandidate` (asserts the other valid outcome: `SetEnabled(false)` committed before the post-fetch recheck → recheck observes disabled and drops, no add fn call, no queue mutation, no broadcast, no history append). Verification: `go test -count=1 ./internal/usecase/roomautoqueue/... ./internal/usecase/roomqueue/... ./internal/delivery/ws/... ./internal/delivery/http/... ./internal/infrastructure/persistence/... ./cmd/server/...` (all pass), `go test -race -count=1 ./internal/usecase/roomautoqueue/... ./internal/usecase/roomqueue/... ./internal/delivery/ws/...` (all pass), `go vet ./internal/... ./cmd/...` (clean), `git diff --check` (clean). No global queue/playback/voting/auto-queue, frontend, Docker, migration-CLI, or REST contract changes. The frontend `RoomView` / store / API surface remains intentionally deferred (R09f is backend-only). The global `/api/autoqueue/...` contract, global `/ws` 16-event inventory, the `auto_queue_config` / `play_history` tables, the `domain.RelatedSongFetcher` / `domain.AutoQueueRepository` / `usecase/autoqueue.Interactor` / `PostgresAutoQueueRepository` / `YtDlpRelatedFetcher`, the global `roomqueue` exclusion of `PrevPlayback` from auto-queue triggers, and the global `queue.AddAutoQueueSong` contract are all unchanged. The next sprint to shape is per the room epic sequence below. **Sprint R09g — Room auto-queue frontend integration** is closed on `dev` (2026-07-01) and accepted by the Product Owner (2026-07-01). R09g exposes the R09f backend runtime in the existing room frontend without changing backend contracts: two thin frontend API methods (`api.getRoomAutoQueueStatus(slug)`, `api.setRoomAutoQueueEnabled(slug, enabled)` — toggle body is exactly `{ enabled }`); an isolated per-room `autoQueueConfig` slice under `globalStore.roomQueues[slug]` (default `{ enabled: false, strategy: 'related' }`) plus three mutators (`setRoomAutoQueueConfig`, `applyRoomAutoQueueAdded` preferring `payload.state`, `applyRoomAutoQueueConfigChanged`); a small `📻 Radio` toggle in RoomView that fetches status on mount + slug change, is disabled when disconnected, unauthenticated, or a toggle is in flight, and maps 401/403/404/409 to clear toasts; and two new WebSocket listeners in RoomView (`room_auto_queue_added`, `room_auto_queue_config_changed`) routed through the per-room mutators. The toggle is a UI convenience only; the backend remains authoritative on host/admin authorization, and the frontend does NOT pre-check the role. The global `/api/autoqueue/...` contract, the global `/ws` 16-event inventory, the global `globalStore.autoQueueConfig` slice, the `frontend/src/views/DashboardView.vue` radio button, and the R09f backend implementation are all unchanged. R09g only touches `frontend/src/services/api.js`, `frontend/src/store/index.js`, `frontend/src/views/RoomView.vue`, and their existing test files; no Go, migration, Docker, or REST/WebSocket contract changes. Verification: `cd frontend && npm run test:unit -- --run` (all pass) and `cd frontend && npm run build` (clean). With R09g's implementation, the next sprint to shape is per the room epic sequence below. Remaining R07 scope (relational queue rows, room-scoped arbitrary drag-and-drop reorder, room-scoped voting, cross-process safety, migration of the legacy global `queue_state` into a room, public read-only API/CORS) remains split and deferred. Earlier sprints (001-010, 011, 020) are recorded in `documents/00-project-management/SPRINTS/`; the recent closed sprint records are `009-sqlite-to-postgresql-data-migration.md` (R03), `010-room-domain-invite-membership-lifecycle.md` (R04, pending PO acceptance), `011-session-token-authentication-authorization.md` (R05), `012-room-scoped-playback-queue.md` (R07a/R07b/R07c/R07d slices), and `020-allowed-origins-and-websocket-origin-policy.md` (A01). **Sprint R10b — Room deletion and membership removal — backend runtime implementation** is implemented on `dev` (2026-07-02) and **accepted by the Product Owner on 2026-07-10**. R10b is the backend-only runtime implementation that conforms to the R10a contract design: two REST endpoints (`DELETE /api/rooms/{slug}` host-only soft archive with idempotent `204` on already-archived rooms; `DELETE /api/rooms/{slug}/members/{userId}` host-only member removal with explicit sentinels `ErrHostCannotRemoveSelf` + `ErrCannotRemoveHost` mapped to `400`, `409` on archived rooms, `404` on non-member target), a single-transaction membership-delete + active-lease-end seam (`RemoveMemberAndEndLeaseAtomic`), three per-room WebSocket envelopes on `/ws/rooms/{slug}` only (`room_member_removed` delivered to the removed client before close, `room_members_changed` delivered to remaining clients, and the per-room-hub `room_archived` broadcast with `reason: "host_archived"` reusing the existing R06 wire value + payload via new explicit per-room hub support), and a removed-client close code of `1008` (policy violation) to distinguish host-driven removal from a benign disconnect. R10b is **backend/runtime only**: no Vue runtime code, no frontend API methods, no frontend store, no RoomView controls, no frontend tests; the frontend UI slice (RoomView delete-room + remove-member controls) is the separate R10c slice. The 16-event global `/ws` inventory, the R06 `room_archived` wire value + payload, the R07b/R07d/R09a/R09b/R09c/R09d/R09f per-room WebSocket events, the R05 in-memory session-token model, the schema version 7 (at the time of R10b acceptance), and all prior sprint runtime surfaces remain byte-for-byte unchanged by R10b. The 49 registered REST endpoints and the 2 WebSocket endpoints reflect the R10b addition. **Sprint R10c — Room deletion and membership removal — frontend UI** is **closed on `dev` (2026-07-10)** and **accepted by the Product Owner (2026-07-10)**. R10c exposes the R10b backend runtime in the existing room frontend with a narrow backend-supported member-list read surface: `GET /api/rooms/{slug}/members` behind `roomAuth` (active-room only; archived maps to 409; any active member may read) with the response body wrapped to `{"members": [{user_id, role}]}` so it matches the R10b `room_members_changed` envelope shape (`joined_at` intentionally omitted per R10a Decision 9). Two thin frontend API methods (`api.deleteRoom(slug)`, `api.removeRoomMember(slug, userId)` with no request body) plus `api.getRoomMembers(slug)` for the initial REST seed; RoomView delete-room and remove-member controls (host-only UI convenience, the backend is authoritative on authorization, frontend does NOT pre-check the role beyond hiding controls); confirmation surface (`window.confirm`) before either destructive call; status-code mapping for 400/401/403/404/409 to clear toasts; an isolated per-room `removed` / `archived` / `members` slice under `globalStore.roomQueues[slug]` plus three mutators (`markRoomArchived` / `markRoomRemovedAsCurrentUser` / `applyRoomMembersChanged`); initial seed via REST on mount and slug change; three new WebSocket listeners in RoomView (`room_archived` with `reason: "host_archived"`, `room_member_removed` targeted at the removed user before close, `room_members_changed` delivered to remaining clients); the per-room WS client now passes the `CloseEvent` to `onClose` and RoomView treats a `1008` close as a removal-driven close (marking the current viewer removed unless the room is already archived); no silent reconnect on any close. R10c only touches `frontend/src/services/api.js`, `frontend/src/services/room-websocket.js`, `frontend/src/store/index.js`, `frontend/src/views/RoomView.vue`, `internal/delivery/http/room_handlers.go` (response wrapper shape for the existing `HandleListMembers`), and their existing test files; no migration, Docker, or other REST/WebSocket contract changes.

## Source-Priority Rule
This `PROJECT_STATE.md` document is the authoritative documentation snapshot for the inspected commit. If any other documentation conflicts with this document, this document is correct regarding the documented state of the codebase. However, if conflicts are discovered between this document and the actual implementation or tests, the implementation and tests themselves remain the ultimate source of truth.

## Architecture/Composition Summary
The project follows a Clean Architecture pattern in Go (Domain, Usecase, Infrastructure, Delivery) for the backend, paired with a Vue 3 frontend using Vite. Real-time updates are handled via WebSocket delta broadcasting. 

## Deployment Topology
Current deployment topology consists of a two-service direct-HTTPS deployment orchestrating via Docker Compose. The Compose service names are `backend` and `frontend`, and their configured container names are `music-queue-backend` and `music-queue-frontend`. The Compose stack also includes the `postgres` service and the `db-init` one-shot job introduced by R02 (and finalized by R03).
- **Port Defaults & Collisions:** The `.env.example` defines sample override values (e.g., 8011, 8012, 1111). However, the `docker-compose.yml` defaults to mapping the host's `443` port for both frontend and backend unless `FRONTEND_HTTPS_PORT` and `BACKEND_PORT` are explicitly overridden, which causes a default host-port 443 collision.
- **Certificate Requirements:** `DUCKDNS_DOMAIN`, `DUCKDNS_TOKEN`, and `LETSENCRYPT_EMAIL` are mandatory for both current container startup scripts. Missing values cause container startup to exit during certificate setup.
- **PostgreSQL Required at Startup:** `DATABASE_URL` (with `POSTGRES_*` overrides) is now mandatory. The backend refuses to start without it; the SQLite runtime fallback was removed in R03. The `backend-db` volume and the SQLite source mount are no longer present in `docker-compose.yml`.

## REST Endpoints
There are exactly 51 registered HTTP REST endpoints plus 2 WebSocket endpoints (`/ws` for the global queue, `/ws/rooms/{slug}` for per-room queue deltas introduced in R07b, playback deltas added in R09a, room vote deltas added in R09b, room playback volume delta added in R09c, room playback previous delta added in R09d, room auto-queue events added in R09f, room deletion + member removal envelopes added in R10b, and room chat envelope added in R11a):
1. `POST /api/auth/google`
2. `POST /api/auth` (Deprecated)
3. `GET /api/queue`
4. `POST /api/queue/add`
5. `POST /api/queue/skip`
6. `POST /api/queue/status`
7. `POST /api/queue/sync`
8. `POST /api/queue/ended`
9. `POST /api/queue/prev`
10. `POST /api/queue/remove`
11. `POST /api/queue/clear`
12. `POST /api/queue/volume`
13. `POST /api/queue/prioritize`
14. `GET /api/user/priority-balance`
15. `GET /api/youtube/search`
16. `POST /api/vote/skip`
17. `POST /api/vote/prioritize`
18. `POST /api/autoqueue/toggle`
19. `GET /api/autoqueue/status`
20. `POST /api/rooms`
21. `GET /api/rooms`
22. `GET /api/rooms/{slug}`
23. `GET /api/rooms/{slug}/members`
24. `POST /api/rooms/{slug}/members/{userId}/promote`
25. `POST /api/rooms/{slug}/members/{userId}/demote`
26. `POST /api/rooms/{slug}/invites`
27. `GET /api/rooms/{slug}/invites`
28. `DELETE /api/rooms/{slug}/invites/{inviteId}`
29. `POST /api/invites/{token}/redeem`
30. `POST /api/rooms/{slug}/player/claim`
31. `POST /api/rooms/{slug}/player/heartbeat`
32. `POST /api/rooms/{slug}/player/release`
33. `GET /api/rooms/{slug}/player/lease`
34. `GET /api/rooms/{slug}/queue`
35. `POST /api/rooms/{slug}/queue/add`
36. `POST /api/rooms/{slug}/queue/remove`
37. `POST /api/rooms/{slug}/queue/clear`
38. `POST /api/rooms/{slug}/queue/prioritize`
39. `POST /api/rooms/{slug}/playback/status`
40. `POST /api/rooms/{slug}/playback/sync`
41. `POST /api/rooms/{slug}/playback/skip`
42. `POST /api/rooms/{slug}/playback/ended`
43. `POST /api/rooms/{slug}/vote/skip` — any active room member, in-memory strict-majority vote session (30s expiry, max(2, n/2 + 1) threshold) for the current song. Missing/empty/non-active-room request maps to `400`. On passed vote the queue advances to the next song and broadcasts room_playback_song_advanced reason=skip. The broadcast payload sanitises the session through a `RoomVoteSessionDTO` that omits `voted_by`, so voter identity is never sent to clients. Bypasses player-lease holder rule.
44. `POST /api/rooms/{slug}/playback/volume` — lease-holder only, body `{"direction":"up"|"down"}`. Returns 204 on success and broadcasts `room_playback_volume_changed` on `/ws/rooms/{slug}`. Volume state is intentionally NOT persisted: there is no `entity.Queue.Volume` field, no `room_queue_state` column, and the command does not call `queueRepo.Save`. The global `/api/queue/volume` contract and the 16-event global `/ws` inventory are byte-for-byte unchanged.
45. `POST /api/rooms/{slug}/playback/prev` — lease-holder only, empty body. Requires a valid current song and `CurrentIndex > 0`. On success decrements `CurrentIndex`, sets `Elapsed = 0`, sets `Status = playing`, persists the room queue, returns 204, and broadcasts `room_playback_song_previous` on `/ws/rooms/{slug}`. On no-current-song or already-first-song returns `400` with no mutation, no save, and no broadcast. Lease sentinels map to 403/404/410 per the R09a contract; archived rooms map to 409. Auto-queue is NOT triggered. The global `/api/queue/prev` contract and the 16-event global `/ws` inventory are byte-for-byte unchanged.
46. `GET /api/rooms/{slug}/autoqueue/status` — any active room member, no lease required. Returns `200` with `{enabled: bool, strategy: "related"}` for the per-room auto-queue config (default `enabled=false`). The per-room setting is independent from the global `auto_queue_config` (toggling the global setting does NOT mutate the per-room setting). 404 on unknown room, 409 on archived room, 403 for non-members. The global `/api/autoqueue/status` contract is byte-for-byte unchanged.
47. `POST /api/rooms/{slug}/autoqueue/toggle` — host or admin role required (NOT player-lease enforced; the toggle is a config edit, not a queue mutation). Body `{"enabled": bool}`. Missing field returns `400`. Returns `200` with the new per-room config on success and broadcasts `room_auto_queue_config_changed` on `/ws/rooms/{slug}`. 403 for guests / non-privileged members, 404 on unknown room, 409 on archived room. The global `/api/autoqueue/toggle` contract and the global `/ws` 16-event inventory are byte-for-byte unchanged.
48. `DELETE /api/rooms/{slug}` — host-only soft archive behind `roomAuth`. Idempotent: `204` on already-archived rooms (no mutation, no broadcast, no audit row). `400` on invalid slug, `401` on missing token, `403` for non-host callers, `404` on missing room. On a successful active → archived transition, invokes `RoomRepository.ArchiveRoomIfActive` (R06) and dispatches `room_archived` on `/ws/rooms/{slug}` with `reason: "host_archived"` (reuses the existing R06 wire value + payload via new explicit per-room hub support; the global `/ws` archive broadcast continues to ride the R06 path unchanged). Hard delete / hard purge / cleanup remain out of scope.
49. `DELETE /api/rooms/{slug}/members/{userId}` — host-only member removal behind `roomAuth`. `400 Bad Request` for `ErrHostCannotRemoveSelf` (`{"error": "host cannot remove self"}`) when the actor is the host removing self, and `ErrCannotRemoveHost` (`{"error": "cannot remove host"}`) when the target is the host of the room; `400` on invalid slug or invalid userId; `401` on missing token; `403` for non-host callers; `404` when target is not a member; `409 Conflict` (`{"error": "room archived"}`) on archived rooms. The membership row delete + active-lease end happen in a single DB transaction (`RemoveMemberAndEndLeaseAtomic`); the room is NOT archived as a side effect of member removal. `room_member_removed` is delivered to the removed user's per-room WebSocket connections before the server closes them with code `1008` (policy violation); `room_members_changed` is broadcast to remaining clients with the post-removal membership list.
50. `GET /api/rooms/{slug}/chat/messages?limit=<1..100>` (R11a, default limit 50) — any active room member behind `roomAuth`. Returns `200` with `{"messages": [{ id, room_slug, sender: { user_id, display_name }, content, created_at }, ...]}` ordered oldest → newest. Out-of-range or non-numeric limits map to `400`. `401` on missing token, `403` for non-members, `404` on unknown slug, `409` on archived rooms. The wire NEVER exposes the sender email; `display_name` falls back to `user #<id>` when the user row's `display_name` is empty.
51. `POST /api/rooms/{slug}/chat/messages` (R11a) — any active room member behind `roomAuth`. Body exactly `{"content": "<plain text>"}`; sender identity is server-resolved from the bearer token, never from the request body. Returns `201` with `{"message": { id, room_slug, sender: { user_id, display_name }, content, created_at }}` (wrapped envelope so the wire shape mirrors the `room_chat_message_created` WS data payload). `400` on invalid slug, invalid limit / content (empty after trim, content > 500 Unicode code points counted via `utf8.RuneCountInString` AFTER trim + CRLF/CR→LF normalization), `401` on missing token, `403` for non-members, `404` on unknown slug, `409` on archived rooms, `500` for unexpected internal errors (generic body, no PII in the response). The wire NEVER exposes the sender email; `display_name` falls back to `user #<id>`. Broadcasts `room_chat_message_created` on `/ws/rooms/{slug}` ONLY after a successful persist; the global `/ws` 16-event inventory is unchanged.

## WebSocket Envelope & Events
WebSocket uses a structured envelope for delta state distribution.
- **Envelope fields:** `type` (string), `data` (JSON payload), `seq_num` (integer), `timestamp` (serialized `time.Time` field).
- **Control Frames:** Ping/pong uses standard WebSocket control frames, not application-level JSON events.
- **Exact 16-event backend application inventory:**
  1. `full_sync`
  2. `user_joined`
  3. `song_added`
  4. `song_skipped`
  5. `status_changed`
  6. `elapsed_sync`
  7. `song_previous`
  8. `song_removed`
  9. `queue_cleared`
  10. `volume_changed`
  11. `song_prioritized`
  12. `priority_balance_updated`
  13. `vote_updated`
  14. `vote_resolved`
  15. `auto_queue_added`
  16. `auto_queue_config_changed`
- **Frontend Compatibility:** The frontend also accepts legacy `queue_updated` and `status_updated` messages.
- **Sprint 004 Additive Fields (review pending):** `song_added` and `auto_queue_added` carry additional authoritative post-mutation fields `current_index`, `current_song`, `status`, and `elapsed`, captured under the same lock as the queue mutation. All pre-existing fields and their JSON tags are unchanged. The frontend applies the additive fields when present; legacy backends omitting them trigger the single approved fallback (first-song promotion when the queue was empty). The Sprint 004 second-pass implementation also adds `previous_current_index` and `playback_advanced` to the internal `AddSongResult` struct returned from the queue interactor — these are NOT serialized over WebSocket. The compatibility claim in this section should be re-verified after Architect review.
- **R05 Auth Posture (WebSocket):** Client-originated messages (e.g. `request_full_sync`) require a valid session presented at connect time as `?session_token=<opaque>`. When present and valid the backend marks the connection `authenticated` and processes client requests. The legacy `?user_id=...` query parameter remains accepted as a non-authenticated daily-priority hint. Connections presenting neither are read-only spectators and any client message they send is rejected.
- **R05 Additive `error` Event:** The backend emits `{"type":"error","data":{"code":"<code>","message":"<message>"}}` when it rejects a client-originated request. This is purely additive — the existing 16-event inventory is unchanged. Pre-existing fields and JSON tags for all other events remain byte-for-byte compatible.
- **R07b Additive Room Queue Events (per-room endpoint only):** `room_queue_sync` (initial on connect), `room_queue_song_added`, `room_queue_song_removed`, `room_queue_cleared`. These ride the new `GET /ws/rooms/{slug}?session_token=<opaque>` route — NOT the global `/ws` endpoint — and are scoped to a single room. The 16-event backend application inventory above is unchanged; R07b events are additive and live on the per-room endpoint. Per-room sequence numbers are independent of the global seq counter. Sequencing is single-process / in-memory; no cross-process ordering claim is made. The `roomqueue.Broadcaster` interface seam in `usecase/roomqueue` keeps the use case independent of `delivery/ws`.
- **R07d Additive Room Queue Event (per-room endpoint only):** `room_queue_song_prioritized` is emitted by the per-room WebSocket hub after a successful `POST /api/rooms/{slug}/queue/prioritize` (host/admin only). The payload carries `from_index`, `to_index`, the post-mutation `song` (with `is_prioritized: true`), and the authoritative `state` snapshot. The 16-event global inventory is unchanged; the event lives only on `GET /ws/rooms/{slug}`.
- **R09a Additive Room Playback Events (per-room endpoint only):** `room_playback_status_changed`, `room_playback_elapsed_sync`, and `room_playback_song_advanced` are emitted by the per-room WebSocket hub after successful `POST /api/rooms/{slug}/playback/{status,sync,skip,ended}` (lease-holder only). `room_playback_status_changed` carries `room_slug`, `status`, `elapsed`, and the authoritative `state` snapshot; `room_playback_elapsed_sync` carries `room_slug`, `elapsed`, and `state`; `room_playback_song_advanced` carries `room_slug`, `reason` ("skip"/"ended"), `previous_index`, `new_index`, `current_song`, `status`, `elapsed`, and `state`. The 16-event global inventory is unchanged; these events live only on `GET /ws/rooms/{slug}`.
- **R09b Additive Room Vote Events (per-room endpoint only):** `room_vote_updated` and `room_vote_resolved` are emitted by the per-room WebSocket hub after each successful `POST /api/rooms/{slug}/vote/skip`. `room_vote_updated` carries `room_slug`, the sanitised `session` (a `RoomVoteSessionDTO` that OMITS `voted_by`), an `actor_user_id` (server-resolved from the bearer token), and the post-mutation queue `state`. `room_vote_resolved` carries `room_slug`, `session_id`, `outcome` ("passed" | "expired"), and `state`. On a passed vote, the existing `room_playback_song_advanced` event is also broadcast with `reason="skip"`. The 16-event global inventory is unchanged; these events live only on `GET /ws/rooms/{slug}`.
- **R09c Additive Room Playback Event (per-room endpoint only):** `room_playback_volume_changed` is emitted by the per-room WebSocket hub after a successful `POST /api/rooms/{slug}/playback/volume` (lease-holder only). Payload: `{room_slug, direction}`. Volume state is intentionally NOT persisted — there is no `entity.Queue.Volume` field, no `room_queue_state` schema change, and the command does not call `queueRepo.Save`. The 16-event global `/ws` inventory and the global `/api/queue/volume` contract are unchanged; this event lives only on `GET /ws/rooms/{slug}`.
- **R09d Additive Room Playback Event (per-room endpoint only):** `room_playback_song_previous` is emitted by the per-room WebSocket hub after a successful `POST /api/rooms/{slug}/playback/prev` (lease-holder only). Payload: `{room_slug, previous_index, new_index, current_song, status, elapsed, state}`. On no-current-song or already-first-song the command returns `400` and the event is NOT broadcast. Lease sentinels map to 403/404/410 per the R09a contract; archived rooms map to 409. Auto-queue is NOT triggered. The 16-event global `/ws` inventory and the global `/api/queue/prev` contract are unchanged; this event lives only on `GET /ws/rooms/{slug}`.
- **Per-room WebSocket inventory (R07b/R07d/R09a/R09b/R09c/R09d/R11a, additive):** `GET /ws/rooms/{slug}` carries the per-room event types — `room_queue_sync` (initial on connect), `room_queue_song_added`, `room_queue_song_removed`, `room_queue_cleared`, `room_queue_song_prioritized`, `room_playback_status_changed`, `room_playback_elapsed_sync`, `room_playback_song_advanced`, `room_vote_updated`, `room_vote_resolved`, `room_playback_volume_changed`, `room_playback_song_previous`, and `room_chat_message_created`. These live on a separate endpoint with independent per-room sequence numbers; they do not appear on the global `/ws` endpoint and the 16-event global inventory is unchanged.
- **R11a Additive Room Chat Event (per-room endpoint only):** `room_chat_message_created` is emitted by the per-room WebSocket hub after a successful `POST /api/rooms/{slug}/chat/messages`. Payload: `{message: { id, room_slug, sender: { user_id, display_name }, content, created_at }}` (the same shape as the POST 201 body, so the frontend routes both through the same store mutator). The wire NEVER exposes the sender email; `display_name` falls back to `user #<id>` when the user row's `display_name` is empty. The 16-event global `/ws` inventory and the global REST routes are unchanged; this event lives only on `GET /ws/rooms/{slug}`.
- **R10b Additive Room Deletion + Member Removal Events (per-room endpoint only):** `room_member_removed`, `room_members_changed`, and the per-room-hub `room_archived` broadcast (with `reason: "host_archived"`). `room_member_removed` is delivered to the removed user's per-room WebSocket connections **before** the server closes them with code `1008` (policy violation); payload `{room_slug, user_id, reason: "host_removed"}`. `room_members_changed` is broadcast to remaining clients after a successful member removal; payload `{room_slug, members: [{user_id, role}]}` (the wire intentionally omits `joined_at`). The per-room-hub `room_archived` broadcast **reuses** the existing R06 wire value + payload (`room_id`, `reason`, `archived_at`); it is delivered only on the active → archived transition (NOT on the idempotent re-call of an already-archived room); the global `/ws` archive broadcast continues to ride the R06 path unchanged. The 16-event global `/ws` inventory is unchanged; these envelopes live only on `GET /ws/rooms/{slug}`.

## Database
PostgreSQL 16 (via `pgx/v5/stdlib`) is the only persistence backend. Schema version is **8** after the embedded migrations run (`0001_initial` + `0002_legacy_id` + `0003_migration_marker` + `0004_rooms` + `0005_player_leases` + `0006_room_queue_state` + `0007_room_auto_queue` + `0008_room_chat_messages`). SQLite is retained ONLY as the offline source reader inside `cmd/migrate-data` / `internal/infrastructure/persistence/migratedata`; the runtime backend, the `DBPath` config field, the `DB_PATH` env fallback, the `backend-db` volume, and the SQLite repository implementations were all removed in R03.
- **Tables:**
  - Core (R01/R02): `queue_state`, `activities`, `users`, `user_sessions`, `priority_transactions`, `auto_queue_config`, `play_history`.
  - Room domain (R04, migration 0004): `rooms`, `room_members`, `room_invites`.
  - Player lease (R06, migration 0005): `player_leases` with the partial unique index `idx_player_leases_one_active_per_room`.
  - Room queue (R07a, migration 0006): `room_queue_state` (single-row JSONB per `rooms.id`, mirror of the global `queue_state` shape).
  - Migration bookkeeping (R03, migration 0003): `migration_marker` (single-row `id=1`).
  - Room auto-queue (R09f, migration 0007): `room_auto_queue_config` (per-room config: `room_id` PK + `enabled` + `strategy` + `updated_at`) and `room_play_history` (per-room play history with the same 50-row per-room cap as the global `play_history` table).
  - Room chat (R11a, migration 0008): `room_chat_messages` (`id BIGSERIAL PK`, `room_id BIGINT NOT NULL FK → rooms.id ON DELETE CASCADE`, `sender_id BIGINT NOT NULL FK → users.id ON DELETE CASCADE`, `content TEXT NOT NULL`, `created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()`) with an index on `(room_id, created_at)` for the per-call history fetch. No retention / cleanup trigger; the chat table is bound to the lifetime of the room under the current R11a scope (R10f+ retention and R11b+ retention purge are both out of R11a scope).
- **Migration identity:** `users.legacy_id BIGINT NULL` (unique index `idx_users_legacy_id`) records the original SQLite `users.id` so dependent tables FK-remap through the new PostgreSQL `users.id` while keeping a stable SQLite-identity audit trail. The column is retained for the audit trail and is NOT dropped by R06 (which added the lease, not a user-identity refactor).
- **Migration marker:** `migration_marker` is a single-row table (`id=1`) carrying per-table SHA256 hashes plus the SQLite file-bytes SHA256 and the source path. It is the durable, exact no-op / idempotency proof for `cmd/migrate-data`.
- **Queue State:** Single-row queue JSON storage defining invariants including active songs, currently playing index. Note: queue JSON activity history is distinct from the separate `play_history` table used by auto-queue. `room_queue_state` is the per-room mirror introduced by R07a.
- **Data migration CLI:** `cmd/migrate-data up` is the operator-driven one-shot, offline, idempotent CLI. Exit codes: 0 = success (including `already migrated; no-op`), 1 = error, 2 = usage. Honors `--sqlite`, `--postgres`, `--report-file`, `--chunk-size`, `--dry-run`. Acquires the `lmq_migration` advisory lock (`pg_try_advisory_lock(987654321)`) on a pinned connection; resyncs BIGSERIAL sequences for `user_sessions`, `priority_transactions`, `activities`, and `play_history` after the copy; writes the `migration_marker` row inside the same transaction; runs a pre-commit in-transaction integrity check.

## Authentication & Authorization
- **Login Verification:** Verifies Google OAuth token audience and `email_verified` fields. Hard-coded allowed domain check. Also logs configured emails (Host/Admin emails) on startup.
- **Identity Management (R05):** Server-issued session tokens (stored in memory) authenticate every privileged REST endpoint via the `Authorization: Bearer <token>` header. Sessions are transient and stored strictly in-memory; they are lost upon server restart and are bound to a single-process/single-instance scope. Client-supplied identity fields (`added_by`, `added_by_id`, `requested_by`, `user_id`, `user_role` in JSON bodies and WebSocket frames) are accepted on the wire for backwards compatibility but are NOT consulted for auth or attribution where R05 changed behavior; the resolved `*entity.User` from the bearer token is the sole source of identity for privileged routes.
- **Authorization Enforcement (R05):** Privileged REST endpoints are server-authenticated and role-gated per the route matrix below. The middleware rejects missing/expired tokens with 401 and role mismatches with 403 before the handler runs.
  - Authenticated-only (any role): `POST /api/queue/add`, `POST /api/queue/remove`, `POST /api/queue/prioritize`.
  - Host or Admin role required: `POST /api/queue/skip`, `POST /api/queue/status`, `POST /api/queue/sync`, `POST /api/queue/ended`, `POST /api/queue/prev`, `POST /api/queue/clear`, `POST /api/queue/volume`, `POST /api/autoqueue/toggle`.
  - Voter or Admin role required: `POST /api/vote/skip`, `POST /api/vote/prioritize`.
  - Unauthenticated (public reads/login): `GET /api/queue`, `GET /api/user/priority-balance`, `GET /api/youtube/search`, `GET /api/autoqueue/status`, `POST /api/auth/google`.
- **Client-Side Compatibility:** Client-supplied `added_by_id` / `user_id` / `user_role` fields in request bodies and WebSocket frames are still parsed and may be persisted for display/audit compatibility, but the authorization gate does not consult them. Frontend and extension clients should not rely on them for auth/identity.

**A01 Allowed Origins (2026-06-26):** `ALLOWED_ORIGINS` is a comma-separated list shared by HTTP CORS and WebSocket upgrades. In `APP_ENV=local` the server falls back to loopback defaults. In any other environment the server refuses to start without a non-empty list. Browser responses echo the allowed origin with `Vary: Origin`; server-to-server requests (empty `Origin`) get no `Access-Control-Allow-Origin` header.

- **WebSocket Caveats (A01):** Both HTTP CORS and WebSocket upgrades are gated by the same configured allow list (`ALLOWED_ORIGINS`). Empty `Origin` is allowed for non-browser/server-to-server traffic but never implies authentication. The legacy `?user_id=...` query parameter is parsed only for diagnostic logging; it does not affect auth, attribution, or daily-priority attribution. Identity requires `?session_token=<opaque>` (R05).
- **Browser Extension (R05 / R06):** `extension/background.js` attaches `Authorization: Bearer <session_token>` to `POST /api/queue/add` whenever the user has saved a token in the extension Options page (storage key `sessionToken` in `chrome.storage.local`). With no token, `/api/queue/add` returns 401 and the extension surfaces a clear "unauthorized" error. R06 added an authenticated in-app **Copy token for extension** button to the web dashboard (success/info/error toast feedback; raw token never enters the DOM or logs), a visible **Token status:** badge on the Options page, and an explicit **Clear Session Token** button that removes only the `sessionToken` key while leaving `apiBase`/`displayName`/`userId` intact. Saving a blank Session Token still preserves the existing saved value.
- **R06 Review Fixes (2026-06-26, closed):** Three small follow-ups landed on `dev` after the R06 review pass — commit `e9fc1a2` (`extension/options.js`) refreshes the "Token status:" badge to "saved" immediately on a non-empty save (blank saves still preserve the existing stored token and leave the badge untouched); commit `5e27789` (`frontend/src/views/DashboardView.vue`) hardens the clipboard-failure catch in `handleCopySessionToken` to log only `err?.name || 'unknown'` instead of the full `Error` object; commit `b01dec0` removes the unapproved R06 plan file `docs/superpowers/plans/2026-06-26-r06-extension-session-token-ux.md`. Verification: `cd frontend && npm run test:unit -- --run` (149/149 pass) and `cd frontend && npm run build` (clean). No backend, REST, WebSocket, storage keys (other than preserving current `sessionToken` semantics), queue, voting, auto-queue, Docker, or deployment changes. This is a small UX/logging closure; the underlying R06 ("Player Lease and Host Departure Semantics") itself is implemented on `dev` (see the R06 row in `ROOM_EPIC_SPRINT_SEQUENCE.md`) with full Product Owner acceptance still pending.

## Community Voting Mechanics
- **Implementation & Persistence:** Vote sessions are stored in-memory.
- **Expiry:** Sessions expire after a 30-second duration.
- **Identity & Threshold (R05):** The REST vote endpoints still parse legacy `user_id` / `user_role` fields in the JSON body for wire compatibility, but they are not consulted for authorization. Backend authorization and the actual vote identity now come from the resolved bearer-token user (see Authentication & Authorization). Connected-client counting still drives threshold calculation; the implemented threshold formula evaluates as `max(2, connectedClients / 2)`, and the threshold is captured at the moment the session is created.
- **Room-scoped vote sessions (R09b, additive):** Vote sessions are stored in-memory in `internal/usecase/roomvote.Interactor`. Sessions are keyed `skip:{roomSlug}:{songID}`, scoped to the current song, expire after 30 seconds, are single-instance only, and never persisted. Threshold = `max(2, uniqueConnectedUserIDs(slug) / 2 + 1)` captured at session creation via `(*RoomWSHub).UniqueConnectedUserIDs(slug)`, which de-duplicates by `userID` so a user with multiple tabs counts once. Payload: the `room_vote_updated` broadcast carries a `RoomVoteSessionDTO`; `voted_by` is never broadcast. The global session map, global vote thresholds, priority balances, and auto-queue are all unchanged.

## Auto-Queue Behavior
- **Trigger:** Activates upon reaching the end of the queue (no-upcoming-song).
- **Execution:** Detached asynchronous execution with an in-process single-flight guard (not a debounce).
- **Configuration & Persistence:** Stores persistent history (`play_history`) and toggled configuration (`auto_queue_config`).
- **Authorization:** Complete lack of backend toggle authorization.

## Testing & CI
### Static Inspection
- `frontend/package.json` exposes Vitest through `test:unit`.
- The non-watch command is `npm run test:unit -- --run`.
- No frontend E2E script was found.

### Command Execution
During the Sprint 003 verification:
- `go test -count=1 ./internal/usecase/auth ./internal/usecase/queue ./internal/delivery/http ./internal/infrastructure/session` - PASS
- `go test -race -count=1 ./internal/usecase/auth ./internal/usecase/queue ./internal/delivery/http ./internal/infrastructure/session` - PASS
- `go test ./...` - BLOCKED (Pre-existing environmental blocker: `open letsencrypt-backend/accounts: permission denied`)
- `go test -race ./...` - BLOCKED (Pre-existing environmental blocker: `open letsencrypt-backend/accounts: permission denied`)
- `go vet ./...` - BLOCKED (Pre-existing environmental blocker: `open letsencrypt-backend/accounts: permission denied`)
- `cd frontend && npm run test:unit -- --run` - PASS (8 test files passed, 28 tests passed)
- `cd frontend && npm run build` - PASS (production build successful)
- `docker compose config` - PASS
- `git diff --check` - PASS
- `git status --short --untracked-files=all` - PASS

## Prioritized Known-Risk Register

1. **Mitigated by R05 / R06 (residual):** Session-token-based identity now authenticates privileged REST endpoints and the WebSocket accept path. Sessions live strictly in-memory and are lost on restart, so a server restart logs every client out and requires re-login. Browser-extension clients must re-paste the token after each new login; no automatic refresh path exists. The web dashboard's Copy button and the Options Clear button make the manual rotation usable and reversible.
2. **Mitigated by R05 (residual):** Privileged REST endpoints are role-gated per the route matrix above. Authorization still relies on the in-memory session, so the same restart-loss caveat applies.
3. **High:** In-memory voting state is lost on restart.
4. **Mitigated by A01 (closed):** WebSocket origins are validated against the shared `ALLOWED_ORIGINS` allow list (same policy as HTTP CORS); the legacy `?user_id=...` hint is parsed only for diagnostic logging and is no longer used for identity or daily-priority attribution.

## Deferred Runtime Sprint Candidates
- Implement secure JWT-based or session-based authentication.
- Enforce backend authorization for all sensitive endpoints.
- Fix broken unit tests.
- Persist vote sessions to SQLite.
- Address Docker Compose port collisions.

## R10a — Room deletion and membership removal — contract design

**Status (2026-07-02): Accepted by the Product Owner. R10a is a docs-only contract design sprint that replaces the legacy R10 stub; NO runtime change ships with this sprint.** R10a introduces **no** Go runtime code, **no** Vue runtime code, **no** migrations, **no** routes, **no** WebSocket constants, **no** config changes, and **no** deployment changes. The contract settles: soft-archive semantics for "delete room" (hard delete / hard purge / cleanup is **out of scope**); host role as the owner equivalent (no new role/owner column); future endpoint contracts `DELETE /api/rooms/{slug}` (host-only, idempotent on archived rooms) and `DELETE /api/rooms/{slug}/members/{userId}` (host-only; cannot remove self; cannot remove host; `409` on archived room); confirmation stance (no body in R10b); HTTP status-code mapping with explicit future sentinels `ErrHostCannotRemoveSelf` and `ErrCannotRemoveHost` for the host-self-remove and remove-host cases (both mapped to `400`); archived-room behavior; member-removal behavior with immediate loss of room membership; session-token stance (no room-scoped revocation — enforce via membership checks + per-room WS disconnect/notification only); player-lease effects (removed active lease holder has lease ended in the same transaction as the membership delete; room archive ends active lease idempotently via the existing R06 path); room-vote effects (removed user MUST NOT count in future thresholds; active sessions are **left to natural expiry**); per-room WebSocket event contracts (no new event-name constant in R10a — the R10b future runtime adds three per-room envelopes on `/ws/rooms/{slug}` only: `room_member_removed` delivered to the removed client before close, `room_members_changed` delivered to remaining clients, and a per-room-hub `room_archived` broadcast reusing the existing R06 wire value + payload — note the existing R06 archive broadcast rides the **global** `*ws.Hub` / global `/ws` endpoint and is NOT currently delivered to per-room clients; R10b must add explicit per-room hub support); an optional audit table shape for the future runtime; and a future test matrix covering usecase, repository, HTTP handlers, WebSocket hub, and race-sensitive lease/vote/member paths. Single-instance only; no cross-process safety claim. The future R10b backend runtime is the next slice and remains deferred; a separate future R10c frontend UI slice is also deferred. See `documents/00-project-management/SPRINTS/015-room-deletion-and-membership-removal.md` for the full contract and `documents/00-project-management/ROOM_EPIC_SPRINT_SEQUENCE.md` for the R10a / R10b / R10c split. The 16-event global `/ws` inventory, the R06 `room_archived` wire value + payload, the R07b/R07d/R09a/R09b/R09c/R09d/R09f per-room WebSocket events, the R05 in-memory session-token model, the schema version 7 (at the time of R10a acceptance), and all prior sprint runtime surfaces remain byte-for-byte unchanged by R10a. The 47 registered REST endpoints and the 2 WebSocket endpoints are unchanged.
