# Active Sprint

There is no currently active sprint on `dev`. The most recently closed and accepted sprint is **R11a — Room chat contract and minimal vertical slice** (closed on `dev` 2026-07-15; accepted by the Product Owner 2026-07-15). R11b+ remains planned and is NOT marked active. R10f+ remains planned and is NOT marked active. R12, R13, and R14 remain planned and are NOT marked active.

Sprint **R11a — Room chat contract and minimal vertical slice** is **closed on `dev` (2026-07-15)** and **accepted by the Product Owner on 2026-07-15**. R11a was implemented on `dev` (2026-07-14) with a corrective pass (2026-07-14), a final corrective pass (2026-07-14), and a deferred lifecycle follow-up (2026-07-15) for the chat bootstrap / recovery guarantees, the 500-Unicode-code-point input behavior, the stale-comment sweep, and the stale-onGap-finalization guard. R11a is the first narrow, full-stack vertical slice of the legacy R11 room-chat bucket: backend (entity, repository, use case, HTTP handlers, additive per-room WebSocket envelope) + frontend (api methods, store mutators, minimal `RoomView` chat panel) + a new PostgreSQL migration (`0008_room_chat_messages`). Active room members can POST a plain-text message and GET recent messages; new persisted messages broadcast over the existing per-room WebSocket stream as an additive `room_chat_message_created` envelope. Sender identity comes from the bearer/session actor (never the request body); room identity comes from the path slug (never the request body). Validation: trim leading/trailing whitespace, normalize CRLF/CR to LF, reject empty content after trim, max 500 Unicode code points (`utf8.RuneCountInString` server-side; `Array.from(str).length` client-side), plain text only (no HTML/rich-text processing). The wire NEVER exposes the sender email; `display_name` falls back to `user #<id>` when the user row's `display_name` is empty. R11a is **intentionally not** the full R11 contract: it ships plain text only, no retention purge, no moderation surface, no audit log, no attachments, no markdown/rich text, no typing indicators, no read receipts, no WebSocket send command, no dedicated chat WebSocket endpoint, and no cross-process guarantees. R11a preserves: the global `/ws` 16-event inventory, the R06 `room_archived` wire value + payload, the R07b/R07d/R09a/R09b/R09c/R09d/R09f/R10b per-room WebSocket events, the R05 in-memory session-token model, the R10a members-list response wrapper, the global room-queue + auto-queue + vote + playback + member-removal surfaces, the R10f+ deferred lifecycle hardening bucket, and the R12/R13/R14 planned sprints. The schema version moves to 8 (after migration 0008); the per-room WebSocket event inventory gains the additive `room_chat_message_created` envelope. R11a touches only the documented R11a files; it does NOT change dependency versions, REST routes unrelated to chat, WebSocket event names/payloads unrelated to chat, store mutators unrelated to chat, runtime code unrelated to chat, Docker/Nginx/deployment, auth/session behavior, or R10f+. The frontend history seed is fired AFTER the first per-room `room_queue_sync` event (the per-room hub's confirmation that the client is registered), NOT on mount before WebSocket registration. The chat input has no native `maxlength` attribute; the input uses `canEditChat` / `canSendChat` so the user can shorten an over-limit draft. The R10e closure is the most recent docs/tooling closure immediately preceding R11a; the R10a contract is preserved byte-for-byte. Verification: `go test -count=1 ./internal/usecase/roomchat/...`, `go test -count=1 ./internal/delivery/http/...`, `go test -count=1 ./internal/delivery/ws/...`, `go test -count=1 ./internal/infrastructure/persistence/...`, `go test -count=1 ./cmd/server/...`, `go test -race -count=1 ./internal/delivery/ws/... ./internal/usecase/roomchat/...`, `cd frontend && npm run test:unit -- --run` (305/305 pass), `cd frontend && npm run build` (clean), `git diff --check` (clean). See [`./016-room-chat-feature.md`](./016-room-chat-feature.md) for the full R11a contract + the deferred R11b+ scope + the closure notes, and [`../ROOM_EPIC_SPRINT_SEQUENCE.md`](../ROOM_EPIC_SPRINT_SEQUENCE.md) for the R11 bucket split (R11a closed/accepted 2026-07-15; R11b+ planned and NOT active).

Sprint **R10e — Roadmap/status refresh after R10d acceptance** is **closed on `dev` (2026-07-10)** and **accepted by the Product Owner on 2026-07-10**. R10e refreshed project-management docs after R10d acceptance by the Product Owner on 2026-07-10: it updated `documents/00-project-management/PROJECT_STATE.md` (current-state summary section + baseline date), `documents/00-project-management/SPRINTS/active.md` (R10d closed/accepted; R10e set as current/active; R11 stayed Planned and was NOT marked active), and `documents/00-project-management/ROOM_EPIC_SPRINT_SEQUENCE.md` (R10d closed/accepted; R10e current; the future bucket renamed from `R10d+` to `R10f+ — Deferred room lifecycle hardening` with its deferred scope preserved: hard delete/retention, admin audit read surface, per-room session revocation, archived-room cleanup; explicitly NOT required before R11 unless the Product Owner decides otherwise). R10e also added minimal frontend tooling metadata so contributors do not run frontend verification on Node 18 by mistake: an `engines.node` field in `frontend/package.json` matching the current frontend dependency baseline `^20.19.0 || ^22.12.0 || >=24.0.0`, and `frontend/.nvmrc` pinned to Node 22. R10e did NOT change dependency versions, dependency lockfile content beyond any lockfile metadata naturally produced by the `engines` field, REST route paths, WebSocket event names or payloads, store mutators, runtime code, migrations, Docker/Nginx/deployment, or R11 (room chat). Verification: `git diff --check`, and frontend checks (`cd frontend && npm run test:unit -- --run`, `cd frontend && npm run build`) only if the available environment is Node 20.19+/22.12+; on Node 18 the frontend checks are blocked by the newly documented Node baseline and that fact is recorded rather than claimed passing. With R10e accepted, the next active sprint is R11a; R10e is recorded here as the most recent docs/tooling closure immediately preceding R11a.

Sprint **R10d — R10c closure polish and docs acceptance** is **closed on `dev` (2026-07-10)** and **accepted by the Product Owner on 2026-07-10**. R10d closed R10c cleanly after Product Owner acceptance: it updated project-management documentation status (marked R10c closed/accepted by the Product Owner on 2026-07-10, moved R10c out of active/current status, set R10d as the current active sprint at the time, and kept R11 Planned and NOT marked active); corrected one narrow frontend empty-state copy in `frontend/src/views/RoomView.vue` so the host member-list no longer says the next `room_members_changed` event will populate the list (R10c now seeds members from `GET /api/rooms/{slug}/members`; new copy is the simple "No members loaded yet."); and added one focused success-path test for the existing `HandleListMembers` HTTP handler in `internal/delivery/http/room_handlers.go` (active room, active actor member; asserts `200`, asserts the response body shape is `{"members": [{"user_id": <number>, "role": "host"|"admin"|"guest"}, ...]}`, and asserts `joined_at` is not present anywhere in the JSON response). R10d preserved the existing 401/403/404/409 behaviour and the existing wire shape (`roomMembersListResp` + `roomMemberWire`). R10d did NOT change R10c runtime behaviour, REST route paths, WebSocket event names or payloads, store mutators, `RoomView` logic other than the empty-state copy, auth/session behaviour, Docker/deployment, migrations, room queue/playback/vote/auto-queue, or R11 (room chat). Verification: `go test -count=1 ./internal/delivery/http/...`, `cd frontend && npm run test:unit -- --run`, `cd frontend && npm run build`, `git diff --check`. With R10d's closure there is no currently active runtime/contract sprint on `dev`; R10e (docs + tooling metadata refresh) is the current active sprint.

Sprint **R10c — Room deletion and membership removal — frontend UI** is **closed on `dev` (2026-07-10)** and **accepted by the Product Owner on 2026-07-10**. R10c exposed the accepted R10b backend runtime in the existing room frontend with a narrow backend-supported member-list read surface for the initial paint: `GET /api/rooms/{slug}/members` behind `roomAuth` (active-room only; archived maps to 409; any active member may read; the per-row shape is exactly `{user_id, role}` matching the R10b `room_members_changed` envelope, with `joined_at` intentionally omitted per R10a Decision 9; no mutation, no broadcast, no client-supplied identity fields); the response body changed from a bare array to a `{"members": [...]}` wrapper so the frontend can route the initial REST read through the same `applyRoomMembersChanged` store mutator as the post-removal WS delta. Frontend-only: two thin frontend API methods (`api.deleteRoom(slug)`, `api.removeRoomMember(slug, userId)` with no request body); `api.getRoomMembers(slug)` for the initial REST seed; RoomView delete-room and remove-member controls (host-only UI convenience, the backend is authoritative on authorization, frontend does NOT pre-check the role beyond hiding controls); confirmation surface (`window.confirm`) before either destructive call; status-code mapping for 400/401/403/404/409 to clear toasts; an isolated per-room `removed` / `archived` / `members` slice under `globalStore.roomQueues[slug]` plus three mutators (`markRoomArchived` / `markRoomRemovedAsCurrentUser` / `applyRoomMembersChanged`); initial seed via REST on mount and slug change; three new WebSocket listeners in RoomView (`room_archived` with `reason: "host_archived"`, `room_member_removed` targeted at the removed user before close, `room_members_changed` delivered to remaining clients); the per-room WS client now passes the `CloseEvent` to `onClose` and RoomView treats a `1008` close as a removal-driven close (marking the current viewer removed unless the room is already archived); no silent reconnect on any close. The R10a / R10b WebSocket contracts, the global `/ws` 16-event inventory, the R09b/R09c/R09d/R09f per-room WebSocket events, the R06 `room_archived` wire value + payload, the R05 in-memory session-token model, the schema version 7, and all prior sprint runtime surfaces remain byte-for-byte unchanged by R10c. R10c only touches `frontend/src/services/api.js`, `frontend/src/services/room-websocket.js`, `frontend/src/store/index.js`, `frontend/src/views/RoomView.vue`, `internal/delivery/http/room_handlers.go` (response wrapper shape for the existing `HandleListMembers`), and their existing test files; no migration, Docker, or other REST/WebSocket contract changes. See [015-room-deletion-and-membership-removal.md](./015-room-deletion-and-membership-removal.md) for the full R10a contract + the R10b implementation summary, and [`../ROOM_EPIC_SPRINT_SEQUENCE.md`](../ROOM_EPIC_SPRINT_SEQUENCE.md) for the R10a / R10b / R10c / R10d split.

Sprint R09g — Room auto-queue frontend integration — is closed on `dev` (2026-07-01) and accepted by the Product Owner (2026-07-01). R09g exposes the R09f backend runtime in the existing room frontend without changing backend contracts: two thin frontend API methods (`api.getRoomAutoQueueStatus(slug)`, `api.setRoomAutoQueueEnabled(slug, enabled)` with body exactly `{ enabled }`); an isolated per-room `autoQueueConfig` slice under `globalStore.roomQueues[slug]` (default `{ enabled: false, strategy: 'related' }`) plus three mutators (`setRoomAutoQueueConfig`, `applyRoomAutoQueueAdded` preferring `payload.state`, `applyRoomAutoQueueConfigChanged`); a small `📻 Radio` toggle in RoomView that fetches status on mount + slug change, is disabled when disconnected, unauthenticated, or a toggle is in flight, and maps 401/403/404/409 to clear toasts; and two new WebSocket listeners in RoomView (`room_auto_queue_added`, `room_auto_queue_config_changed`) routed through the per-room mutators. The toggle is a UI convenience only; the backend remains authoritative on host/admin authorization. The global `/api/autoqueue/...` contract, the global `/ws` 16-event inventory, the global `globalStore.autoQueueConfig` slice, the global dashboard's radio button, the R09f backend implementation, and the R09f schema version 7 (`room_auto_queue_config` + `room_play_history`) are all unchanged. R09g only touches `frontend/src/services/api.js`, `frontend/src/store/index.js`, `frontend/src/views/RoomView.vue`, and their existing test files. Verification: `cd frontend && npm run test:unit -- --run` (all pass) and `cd frontend && npm run build` (clean). R10b is the shaped next sprint; the R10b runtime implementation will conform to the accepted R10a contract.

Sprint R09f — Room auto-queue backend runtime — is closed on `dev` (2026-07-01) and accepted by the Product Owner (2026-07-01). R09f is the backend-only runtime implementation of the room auto-queue contract shaped by R09e. R09f adds two REST endpoints (`GET /api/rooms/{slug}/autoqueue/status`, `POST /api/rooms/{slug}/autoqueue/toggle` behind `roomAuth` with host/admin toggle authorization enforced at the use-case layer); two per-room WebSocket events (`room_auto_queue_added`, `room_auto_queue_config_changed`) on `/ws/rooms/{slug}` only — the global `/ws` 16-event inventory remains byte-for-byte unchanged; a new `internal/usecase/roomautoqueue` package that orchestrates per-room config read + yt-dlp recommendation fetch + queue-owned conditional insertion + history append + per-room broadcast; per-room auto-queue persistence (migration 0007_room_auto_queue: `room_auto_queue_config` + `room_play_history` with the same 50-row per-room cap as the global `play_history`); a queue-owned conditional insertion seam in `roomqueue.Interactor.AddRoomAutoQueueSong` that returns `ErrRoomAutoQueueStale` on any violation with zero side effects (mirrors the global `queue.AddAutoQueueSong` contract byte-for-byte); a non-blocking `CheckAndTrigger` wired into `PlaybackEnded` / `SkipPlayback` / `SkipVote` / `RemoveSong` / `ClearQueue` (when the post-mutation queue ends at `current==last`); a per-room `inFlight` map guarded by a coordinator mutex whose critical section is narrow enough to preserve cross-room independence (the slow FetchRelated call is held outside any lock); a typed narrow broadcaster seam (replacing the prior generic-map BroadcastFunc adapter) that the production wiring connects directly to `*ws.RoomWSHub.BroadcastRoomAutoQueueAdded` and that carries the full R09e payload tuple (room_slug, song, source_song_title, current_index, current_song, status, elapsed, state); and `SetEnabled` serialized with the post-fetch insertion so a disable that lands before insertion drops the candidate with no queue save, no history append, and no broadcast. The closure pass tightened the `SetEnabled` ↔ `CheckAndTrigger` serialization invariant on `dev`: `SetEnabled`'s `SaveConfig` now runs under the same coordinator `mu` that the post-fetch recheck + `AddRoomAutoQueueSong` insertion hold, so a `SetEnabled(false)` save can no longer interleave between the final "still enabled?" check and the queue mutation (the prior code resolved room + checked role + called `GetConfig`/`SaveConfig` without ever taking the coordinator mutex). The disabled-mid-flight test was replaced with two tests that pin the locked-correct outcomes directly — `TestSetEnabled_SerializedWithPostFetchCriticalSection` (asserts `SaveConfig` does not enter while the add fn holds mu, then proceeds after release) and `TestSetEnabled_RunsBeforePostFetchRecheck_DropsCandidate` (asserts the other valid outcome: `SetEnabled(false)` committed before the post-fetch recheck → recheck observes disabled and drops, no add fn call, no queue mutation, no history append, no broadcast). The package-level `Locking discipline` comment and `Interactor.mu` doc reflect the new serialization surface; `GetConfig` (and all other slow paths) remain outside the mutex. The frontend `RoomView` / store / API surface remains intentionally deferred (R09f is backend-only). See [021-room-auto-queue-contract-design.md](./021-room-auto-queue-contract-design.md) for the contract and the *Implementation summary (R09f)* section.

Sprint R09e — Room auto-queue contract design (planning/design only) — was closed and accepted on 2026-07-01 just before R09f. R09e is a documentation-only planning/design sprint that shapes the contract. R09e introduces no Go runtime code, no Vue runtime code, no migrations, no routes, no WebSocket constants, and no config changes. The contract settles per-room scope (room-scoped, NOT silently controlled by the global auto-queue config), per-room persistence (config + history tables or a documented equivalent), reuse of the existing `domain.RelatedSongFetcher` abstraction, a future `roomautoqueue.Interactor` orchestrator that uses a coordinator `mu` mutex guarding a per-room `inFlight` map keyed by room id/slug (NOT a single global `triggering` flag), queue-owned conditional insertion + stale revalidation in `roomqueue.Interactor`, a per-room `room_auto_queue_added` event on `/ws/rooms/{slug}` only (the global `/ws` 16-event inventory is unchanged), per-room single-flight concurrency with cross-room independence, zero-side-effect stale-candidate handling, host/admin toggle authorization, and a full future test matrix. Single-instance only; no cross-process safety claim. See [021-room-auto-queue-contract-design.md](./021-room-auto-queue-contract-design.md). R09f is now the runtime implementation slice that conforms to R09e; the next slice is per the room epic sequence below.

Sprint R10b — Room deletion and membership removal — backend runtime implementation is **closed on `dev` (2026-07-02)** and **accepted by the Product Owner on 2026-07-10**. R10b is the backend-only runtime implementation that conforms to the accepted **R10a — Room deletion and membership removal — contract design** (Accepted 2026-07-02). R10a is a **documentation-only** contract design sprint: it introduces **no** Go runtime code, **no** Vue runtime code, **no** migrations, **no** routes, **no** WebSocket constants, and **no** config changes. R10a replaces the legacy R10 stub with a narrow, reviewable contract. R10b implements that contract by landing the two HTTP routes (`DELETE /api/rooms/{slug}` host-only soft archive, idempotent on already-archived rooms; `DELETE /api/rooms/{slug}/members/{userId}` host-only member removal with explicit sentinels `ErrHostCannotRemoveSelf` + `ErrCannotRemoveHost` mapped to `400`, `409` on archived rooms, `404` on non-member target), the membership-delete + active-lease-end atomic transaction, the three per-room WebSocket envelopes on `/ws/rooms/{slug}` only — `room_member_removed` (delivered to the removed client before close), `room_members_changed` (delivered to remaining clients), and the per-room-hub `room_archived` broadcast (with `reason: "host_archived"`, reusing the existing R06 wire value + payload via new explicit per-room hub support; the global `/ws` archive broadcast continues to ride the R06 path unchanged) — and a removed-client WebSocket close with code `1008` (policy violation) to distinguish host-driven removal from a benign disconnect. R10b is **backend/runtime only**: no Vue runtime code, no frontend API methods, no frontend store, no RoomView controls, no frontend tests. The frontend UI slice for delete-room / remove-member controls is **R10c** (the active sprint). The R10a contract settles: soft-archive semantics for "delete room" (hard delete / hard purge / cleanup is **out of scope**); host role as the owner equivalent (no new role/owner column); confirmation stance (no body in R10b — confirmed by bearer-token + host role); HTTP status-code mapping with explicit sentinels `ErrHostCannotRemoveSelf` and `ErrCannotRemoveHost` (both mapped to `400`); archived-room behavior; member-removal behavior with immediate loss of room membership; session-token stance (no room-scoped revocation — enforce via membership checks + per-room WS disconnect/notification only); player-lease effects (removed active lease holder has lease ended in the same transaction as the membership delete; room archive ends active lease idempotently via the existing R06 path); room-vote effects (removed user MUST NOT count in future thresholds; active sessions are **left to natural expiry**); an optional audit table shape (R10b does NOT introduce the table); and the Decision 11 test matrix (covered by R10b usecase / HTTP handler / WebSocket hub / race-sensitive tests). Single-instance only; no cross-process safety claim. See [015-room-deletion-and-membership-removal.md](./015-room-deletion-and-membership-removal.md) for the full R10a contract + the R10b Implementation summary section, and [`../ROOM_EPIC_SPRINT_SEQUENCE.md`](../ROOM_EPIC_SPRINT_SEQUENCE.md) for the R10a / R10b / R10c split.

The sprint before R09e is **Sprint R09d — Room-scoped previous playback command** (closed 2026-07-01, accepted by the Product Owner 2026-07-01). R09d adds one lease-holder-only HTTP slice (`POST /api/rooms/{slug}/playback/prev`, behind `roomAuth`, empty body), one additive per-room WebSocket event (`room_playback_song_previous`), and a minimal frontend Prev control surface in RoomView (gated on the existing lease-holder UI affordance AND on `current_index > 0`). The command requires a valid current song and `CurrentIndex > 0`; on success it decrements `CurrentIndex`, sets `Elapsed = 0`, sets `Status = playing`, and persists the room queue. On no-current-song or already-first-song the command returns `400` with no mutation, no save, and no broadcast. A new `entity.Queue.PrevToPrevious` helper mirrors `AdvanceToNext`'s no-partial-mutation invariant, and a local `roomqueue.ErrNoPreviousSong` sentinel is added so the handler maps cleanly to 400 without string-matching the legacy `Queue.Prev()` error. Auto-queue is NOT triggered. The global `/api/queue/prev` contract, the global `/ws` 16-event inventory, voting, priority balances, Docker, CORS, and auth/session design remain untouched. See the *Implementation summary (R09d)* section in [014-player-control-semantics.md](./014-player-control-semantics.md).

Sprint 016 / R09c — Room-scoped volume command is closed (2026-06-30, accepted by the Product Owner 2026-07-01). R09c adds one backend-only HTTP slice (`POST /api/rooms/{slug}/playback/volume`, behind `roomAuth`), one additive per-room WebSocket event (`room_playback_volume_changed`), and a narrow frontend wiring (one API method + one listener + a small Vol± control surface in RoomView, gated by the same lease-holder UI affordance as the R09a playback panel). Volume is intentionally NOT persisted: there is no `entity.Queue.Volume` field, no `room_queue_state` schema change, and the command does not call `queueRepo.Save`. The global `/api/queue/volume` contract, the global `/ws` 16-event inventory, voting, priority balances, auto-queue, Docker, CORS, auth/session design remain untouched. The closure pass tightened two narrow UX details on `dev`: the frontend toast on `room_playback_volume_changed` now uses an explicit up/down branch (a non-up/down payload reads as a neutral "Volume changed." rather than silently defaulting to "decreased"); and the WS hub test file ends with a trailing newline matching the rest of the suite. See the *Implementation summary (R09c)* section in [014-player-control-semantics.md](./014-player-control-semantics.md).

Sprint 015 / R09b — Room vote-to-skip backend contract is closed (2026-06-30, accepted by the Product Owner). R09b adds one backend-only HTTP slice (`POST /api/rooms/{slug}/vote/skip`, behind `roomAuth`), two additive per-room WebSocket events (`room_vote_updated`, `room_vote_resolved`), and an additive reuse of the existing `room_playback_song_advanced` event (with `reason: "skip"`) when the vote passes. Vote sessions live in a new `internal/usecase/roomvote` package: in-memory, room-scoped, current-song-scoped, 30-second expiry, single-instance only, never persisted. Threshold is captured at session creation via a new `(*RoomWSHub).UniqueConnectedUserIDs(slug) int` (de-duplicates by `userID`) using strict-majority `max(2, n/2 + 1)` with a 2-voter minimum. The global `/api/vote/skip`/`/api/vote/prioritize`, global `/api/queue/...`, global `/ws` 16-event inventory, vote thresholds, priority balances, auto-queue, Docker, CORS, auth/session design, and frontend UI remain untouched. See the *Implementation summary (R09b)* section in [014-player-control-semantics.md](./014-player-control-semantics.md).

Sprint 014 / R09a — Lease-aware room playback controls is closed (2026-06-30, accepted by the Product Owner). See [014-player-control-semantics.md](./014-player-control-semantics.md) for the sprint record.

Sprint 020 / A01 — Allowed Origins and WebSocket Origin Policy is closed (2026-06-26, pending Product Owner acceptance). A01 introduced a shared `ALLOWED_ORIGINS` policy shared by HTTP CORS and WebSocket upgrades, with fail-fast validation outside `APP_ENV=local`, and deprecated the legacy `?user_id=...` WebSocket query parameter as an identity or daily-priority attribution signal. See [020-allowed-origins-and-websocket-origin-policy.md](./020-allowed-origins-and-websocket-origin-policy.md).

Sprint R05 — Session Token Authentication & Authorization is closed. R05 shipped server-issued in-memory session tokens, `Authorization: Bearer <token>` enforcement on privileged REST endpoints, role-gated authorization, `?session_token=<opaque>` WebSocket authentication, an additive `error` event for rejected client-originated requests, and browser-extension session-token handling. See `../PROJECT_STATE.md` for the authoritative R05 state description.

Sprint 009 / R03 — SQLite-to-PostgreSQL Data Migration is closed. See [009-sqlite-to-postgresql-data-migration.md](./009-sqlite-to-postgresql-data-migration.md).

Sprint 008 / R02 — PostgreSQL Foundation with Existing Behavior Preserved is closed. See [008-postgresql-foundation-with-existing-behavior-preserved.md](./008-postgresql-foundation-with-existing-behavior-preserved.md).

Sprint 007 / R01 — PostgreSQL Migration Design is closed. See [007-postgresql-migration-design.md](./007-postgresql-migration-design.md).

Sprint 006 / R00 — Room Architecture ADR / R00 is closed. See [006-room-architecture-adr-contract-plan.md](./006-room-architecture-adr-contract-plan.md).

The next sprint to shape after R09f acceptance is the R09f+ remaining R09 bucket — full media-player device integration (and any other R09 scope not yet covered by R09a/R09b/R09c/R09d/R09e/R09f) — per [`../ROOM_EPIC_SPRINT_SEQUENCE.md`](../ROOM_EPIC_SPRINT_SEQUENCE.md). R06 (Player Lease and Host Departure Semantics) is implemented on `dev` with full Product Owner acceptance pending; R07a/R07b/R07c/R07d, R09a, R09b, R09c, R09d, R09e, and R09f are all closed and accepted; R05 (session token authentication & authorization) and A01 (allowed origins & WebSocket origin policy) were completed out-of-band of the R04 sequence.

*Superseded:* R09d is closed and accepted (2026-07-01) and R09e — Room auto-queue contract design — is also closed and accepted (2026-07-01; see the top of this file). The "R09d+" bucket language above pre-dates R09d's, R09e's, and R09f's closure. R09f — Room auto-queue backend runtime — is also closed and accepted (2026-07-01; see the top of this file).

Sprint 012 / R07a — Room-scoped playback queue (narrow first slice) is closed (2026-06-29, accepted by the Product Owner). See [012-room-scoped-playback-queue.md](./012-room-scoped-playback-queue.md).

Sprint 012 / R07b — Room-scoped playback queue (per-room WebSocket deltas at `GET /ws/rooms/{slug}`) is closed (2026-06-29, accepted by the Product Owner). See [012-room-scoped-playback-queue.md](./012-room-scoped-playback-queue.md). Full R07 remains split; the remaining slices (relational queue rows, room-scoped reorder/vote endpoints, cross-process safety, and migration of the legacy global `queue_state` into a room) are deferred per the 012 document. The original "R08 placeholder" (public read-only API and CORS) was renumbered away from R07b and remains a future sprint per the sequence doc.

Sprint 012 / R07c — Room-scoped playback queue (frontend narrow wiring for R07a/R07b) is closed (2026-06-29, accepted by the Product Owner). See [012-room-scoped-playback-queue.md](./012-room-scoped-playback-queue.md).

Sprint 012 / R07d — Room-scoped playback queue (one room-scoped mutation: POST /api/rooms/{slug}/queue/prioritize, plus the matching per-room WebSocket delta and a minimal RoomView Prioritize control) is closed (2026-06-30, accepted by the Product Owner). The closure pass tightened three narrow invariants from the original R07d landing: the handler request shape uses `*int` so a missing `song_index` is rejected with `400`; the interactor returns the post-mutation song so the broadcast payload carries `IsPrioritized=true`; and `PROJECT_STATE.md` plus the frontend store fallback reflect the snake-case `is_prioritized` field. R07d does not advance any out-of-scope work (no global route, no priority balances, no relational rows). See the *Implementation summary (R07d)* and *Closure* sections in [012-room-scoped-playback-queue.md](./012-room-scoped-playback-queue.md).
