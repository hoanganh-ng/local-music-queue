# Room Epic – Sprint Sequence

This document lays out the high‑level sequence of sprints for the **Local Music Queue** room epic.  Each sprint is scoped narrowly and builds upon the preceding work.  As sprints close, their status will be updated in this document along with a short description of the outcome.  If a sprint has not yet been shaped, its goals and requirements are captured in a corresponding stub in the `SPRINTS` folder.

| Sprint | Name | Status | Description |
|---|---|---|---|
| **R00** | Room architecture contract plan | **Closed** | Established the baseline architecture contract for rooms. Defined key domain concepts, roles (host, member, guest), and the high‑level API surface. Captured as ADR 001 and sprint `006-room-architecture-adr-contract-plan`. |
| **R01** | PostgreSQL migration design | **Closed** | Designed the move from an embedded SQLite database to PostgreSQL. Documented migration strategy, updated schemas and connection configuration. Captured as ADR 002 and sprint `007-postgresql-migration-design`. |
| **R02** | Core domain implementation | **Closed** | Implemented initial room domain entities (room, user, membership) and basic REST endpoints. Laid groundwork for future sprints by scaffolding repository interfaces and service layers. |
| **R03** | SQLite → PostgreSQL data migration | **Closed** | Performed a one‑time migration of all persistent data from SQLite to PostgreSQL. Added migration scripts, validated data integrity and adjusted CI pipelines accordingly. |
| **R04** | Room invites and membership lifecycle | **Closed** | Added support for inviting users to a room, accepting/declining invites, leaving a room and transferring ownership. Introduced roles (owner vs. member) and access control for invite and join operations. |
| **R05** | Session token authentication | **Closed** | Introduced per‑user session tokens for authenticating both REST and WebSocket calls. Added middleware to resolve a user from a session token, updated the invite/join flows to issue tokens, and tightened CORS. |
| **R06** | Player lease and host departure semantics | **Implemented on `dev` (2026-06-26); post-acceptance UX follow-ups closed on `dev`; full Product Owner acceptance pending** | Introduced the `PlayerLease` concept that formalizes who controls playback. Added claim / heartbeat / release / get REST endpoints under `/api/rooms/{slug}/player/...` behind bearer-token auth, the partial-unique-index `player_leases` table (migration 0005, schema version 5), the additive `room_archived` WebSocket event (16-event global inventory unchanged), the 5-second sweep ticker that ends leases past grace and archives the room exactly once, and the `RoomArchivedBroadcaster` seam that keeps `usecase/room` independent of `delivery/ws`. Three small post-acceptance UX/logging follow-ups landed on `dev` (commits `e9fc1a2`, `5e27789`, `b01dec0`) — Options-page token-status badge refresh, dashboard clipboard-failure catch hardening, and removal of an unapproved plan file. No global queue/playback/voting/auto-queue, frontend, Docker, or migration-CLI changes. Documented in `011-player-lease-and-host-departure-semantics.md` (Implementation summary + Verification Results sections). |
| **R07** | Room‑scoped playback queue | **R07a closed (2026-06-29); R07b closed (2026-06-29); R07c closed (2026-06-29); R07d closed (2026-06-30); all accepted by the Product Owner; rest split/deferred** | R07a landed per-room queue persistence plus the four room-scoped REST endpoints (add/remove/clear/get). R07b landed the additive per-room WebSocket sync/delta events at `GET /ws/rooms/{slug}` and the `roomqueue.Broadcaster` seam, plus fixes to the per-room hub keepalive (added ping ticker), register/sync ordering (hub-loop linearisation of seq allocation), and the matching documentation. R07c landed the narrow authenticated frontend wiring (`/rooms/:slug` route + minimal `RoomView.vue`; four `api.*Room*` REST methods; dedicated `room-websocket.js` client with sequence-gap recovery via REST; isolated `globalStore.roomQueues` slice) against the R07a/R07b contracts without changing backend behavior. R07d landed one room-scoped queue mutation: `POST /api/rooms/{slug}/queue/prioritize`, the matching `room_queue_song_prioritized` per-room WebSocket delta, and a minimal RoomView Prioritize control (host/admin only), with tightened closure-pass invariants (`*int` request shape so missing `song_index` returns 400; interactor returns the post-mutation song so the broadcast payload carries `is_prioritized: true`). No changes to global queue behavior or the global `/ws` contract were made. Remaining R07 scope (relational queue rows, room-scoped arbitrary drag-and-drop reorder, room-scoped voting, cross-process safety, migration of the legacy global `queue_state` into a room) remains split and deferred to subsequent slices. The original R08 placeholder ("Public read-only API and CORS") is deferred/renumberable; it is NOT what R07b shipped. See sprint `012-room-scoped-playback-queue.md`. |
| **R08** | Public read‑only API and CORS | **Planned** | Expose read‑only endpoints for external clients to fetch room state (queue, player status) without requiring full authentication. Implement strict CORS policies and caching headers. See sprint `013-public-read-only-api-and-cors.md`. |
| **R09a** | Lease-aware room playback controls | **Accepted (2026-06-30)** | First narrow slice of R09. Adds four lease-holder-only room playback mutations — `POST /api/rooms/{slug}/playback/{status,sync,skip,ended}` — plus three additive per-room WebSocket events (`room_playback_status_changed`, `room_playback_elapsed_sync`, `room_playback_song_advanced`) on `/ws/rooms/{slug}` and a minimal RoomView Play/Pause/Skip/Ended control surface. Lease enforcement at the use-case layer via a new `room.PlaybackLeaseAuthorizer` seam implemented by `PlayerLeaseInteractor.RequireActiveLeaseHolder`. The global `/api/queue/...` contract, the 16-event global `/ws` inventory, voting, auto-queue, public API/CORS, Docker, and auth remain untouched. See sprint `014-player-control-semantics.md` Implementation summary (R09a). |
| **R09b** | Room vote-to-skip backend contract | **Accepted (2026-06-30)** | First narrow slice of the legacy R09b+ bucket. Adds `POST /api/rooms/{slug}/vote/skip` (behind `roomAuth`), the additive per-room WS events `room_vote_updated` + `room_vote_resolved`, a unique-connected-presence counter on `*RoomWSHub`, and reuses the existing `room_playback_song_advanced` event with `reason: "skip"` on a passed vote. Sessions live in `internal/usecase/roomvote` — in-memory, room-scoped, current-song-scoped, 30-second expiry, single-instance only. Threshold = `max(2, uniqueConnectedUserCount / 2 + 1)` captured at session creation. Wire updates: production-time ExpireSessions runner is now ticking on /ws/rooms/{slug} via a server-owned adapter; the room-vote broadcast payload sanitises the session through a RoomVoteSessionDTO that omits VotedBy so `voted_by` is never sent to clients. Empty queue / no current song maps to `400 Bad Request`. The global `/api/vote/...`, global `/api/queue/...`, global `/ws` 16-event inventory, priority balances, auto-queue, Docker, CORS, auth, and frontend UI remain untouched. See sprint `014-player-control-semantics.md` Implementation summary (R09b). |
| **R09c** | Room-scoped volume command | **Closed (2026-07-01, accepted by the Product Owner)** | First narrow slice of the legacy R09c+ bucket. Adds one lease-holder-only room-scoped command — `POST /api/rooms/{slug}/playback/volume` with body `{"direction":"up"|"down"}` — plus one additive per-room WebSocket event (`room_playback_volume_changed`) on `/ws/rooms/{slug}`. Lease enforcement at the use-case layer via the existing `room.PlaybackLeaseAuthorizer` seam (R09a). Volume is intentionally NOT persisted: there is no PostgreSQL migration, no `room_queue_state` JSON shape change, no `entity.Queue.Volume` field, and the command does not call `queueRepo.Save`. The global `/api/queue/volume` contract, the 16-event global `/ws` inventory, voting, auto-queue, public API/CORS, Docker, and auth remain untouched. The closure pass tightened two narrow UX details on `dev`: the frontend toast on `room_playback_volume_changed` now uses an explicit up/down branch (a non-up/down payload reads as a neutral "Volume changed." rather than silently defaulting to "decreased"); and the WS hub test file ends with a trailing newline matching the rest of the suite. See sprint `014-player-control-semantics.md` Implementation summary (R09c). |
| **R09d** | Room-scoped previous playback command | **Closed (2026-07-01, accepted by the Product Owner)** | Second narrow slice of the legacy R09c+ bucket (the original `prev` slice from the R09 sprint stub). Adds one lease-holder-only room-scoped command — `POST /api/rooms/{slug}/playback/prev` (empty body) — plus one additive per-room WebSocket event (`room_playback_song_previous`) on `/ws/rooms/{slug}`. Lease enforcement at the use-case layer via the existing `room.PlaybackLeaseAuthorizer` seam (R09a). The command requires a valid current song and `CurrentIndex > 0`; on success it decrements `CurrentIndex`, sets `Elapsed = 0`, sets `Status = playing`, and persists the room queue. On no-current-song or already-first-song the command returns `400` with no mutation, no save, and no broadcast. Lease sentinels map to 403/404/410/409 per the R09a contract; archived rooms map to 409. A new `entity.Queue.PrevToPrevious` helper mirrors `AdvanceToNext`'s no-partial-mutation invariant and a local `roomqueue.ErrNoPreviousSong` sentinel is added so the handler maps cleanly to 400 without string-matching the legacy `Queue.Prev()` error. Auto-queue is NOT triggered. The global `/api/queue/prev` contract, the 16-event global `/ws` inventory, voting, priority balances, Docker, CORS, and auth/session design remain untouched. A minimal frontend `Prev` control is gated on the existing lease-holder UI affordance AND on `current_index > 0` (UI convenience only; backend is authoritative). See sprint `014-player-control-semantics.md` Implementation summary (R09d). |
| **R09e** | Room auto-queue contract design | **Closed (2026-07-01, accepted by the Product Owner)** | Third narrow slice of the legacy R09c+ bucket (the room auto-queue shape from the R09 sprint stub). R09e is a **documentation-only** planning/design sprint that shapes a narrow, reviewable contract for a future room auto-queue implementation. R09e introduces **no** Go runtime code, **no** Vue runtime code, **no** migrations, **no** routes, **no** WebSocket constants, and **no** config changes. The contract settles: per-room scope (room-scoped, NOT silently controlled by the global auto-queue config), per-room persistence (per-room config + history tables or a documented equivalent), reuse of the existing `domain.RelatedSongFetcher` abstraction, a future `roomautoqueue.Interactor` orchestrator that uses a coordinator `mu` mutex guarding a per-room `inFlight` map keyed by room id/slug (NOT a single global `triggering` flag) and that NEVER holds a lock during the slow `FetchRelated` call, queue-owned conditional insertion + stale revalidation in `roomqueue.Interactor`, a per-room `room_auto_queue_added` event on `/ws/rooms/{slug}` only (the global `/ws` 16-event inventory is unchanged), per-room single-flight concurrency with cross-room independence mirroring the global auto-queue's Sprint 004 hardening, zero-side-effect stale-candidate handling (no save, no history, no activity, no broadcast), host/admin toggle authorization, failure semantics, and a full future test matrix. Single-instance only; no cross-process safety claim. The future room auto-queue runtime implementation is the next R09e+ slice and remains deferred. See sprint `021-room-auto-queue-contract-design.md`. |
| **R09f** | Room auto-queue backend runtime | **Closed (2026-07-01, accepted by the Product Owner)** | Fourth narrow slice of the legacy R09c+ bucket (the room auto-queue runtime). R09f is a **backend-only** runtime implementation that conforms to the R09e contract. R09f lands two REST endpoints (`GET /api/rooms/{slug}/autoqueue/status` for any active member; `POST /api/rooms/{slug}/autoqueue/toggle` for host/admin only) behind `roomAuth`; two per-room WebSocket events (`room_auto_queue_added`, `room_auto_queue_config_changed`) on `/ws/rooms/{slug}` only — the global `/ws` 16-event inventory remains byte-for-byte unchanged; a new `internal/usecase/roomautoqueue` package that orchestrates per-room config read + yt-dlp recommendation fetch + queue-owned conditional insertion + history append + per-room broadcast; per-room auto-queue persistence via migration `0007_room_auto_queue` (`room_auto_queue_config` + `room_play_history` with the same 50-row per-room cap as the global `play_history`); a queue-owned conditional insertion seam in `roomqueue.Interactor.AddRoomAutoQueueSong` that returns `roomqueue.ErrRoomAutoQueueStale` on any violation with zero side effects (mirrors the global `queue.AddAutoQueueSong` contract byte-for-byte); a non-blocking `CheckAndTrigger` wired into `PlaybackEnded` / `SkipPlayback` / `SkipVote` / `RemoveSong` / `ClearQueue` only when the post-mutation queue ends at `current==last`; a per-room `inFlight` map guarded by a coordinator mutex whose critical section is narrow enough to preserve cross-room independence (the slow FetchRelated call is held outside any lock); a typed narrow broadcaster seam (the production wiring connects the use case to `*ws.RoomWSHub.BroadcastRoomAutoQueueAdded` directly, carrying the full R09e payload tuple — room_slug, song, source_song_title, current_index, current_song, status, elapsed, state); and `SetEnabled` serialized with the post-fetch insertion so a disable landing before the insertion drops the candidate with no queue save, no history append, and no broadcast. The frontend `RoomView` / store / API surface is intentionally deferred (R09f is backend-only per the sprint directive). The global `/api/autoqueue/...` contract, the global `/ws` 16-event inventory, the global `auto_queue_config` / `play_history` tables, the `domain.AutoQueueRepository` / `usecase/autoqueue.Interactor` / `PostgresAutoQueueRepository` / `YtDlpRelatedFetcher`, the global `roomqueue` exclusion of `PrevPlayback` from auto-queue triggers, and the global `queue.AddAutoQueueSong` contract are all unchanged. Authorization is owned at the use-case layer (`GetConfig` is open to any active member; `SetEnabled` requires host/admin via `requireHostOrAdmin`), matching the roomqueue use-case pattern. **Closure pass (2026-07-01):** tightened the `SetEnabled` ↔ `CheckAndTrigger` serialization invariant — `SetEnabled`'s `SaveConfig` write now runs under the same coordinator `mu` that the post-fetch recheck + `AddRoomAutoQueueSong` insertion hold, so a `SetEnabled(false)` save can no longer interleave between the final "still enabled?" check and the queue mutation. `GetConfig` (and the rest of the slow paths in both methods) remains outside the mutex; the critical section is the minimum needed to serialize the disable write with the post-fetch decision window. The package-level `Locking discipline` comment and the `Interactor.mu` doc reflect the new serialization surface. The disabled-mid-flight test was replaced with two tests that pin the locked-correct outcomes directly — `TestSetEnabled_SerializedWithPostFetchCriticalSection` (RED-fails without the lock; asserts `SaveConfig` does not enter while the add fn holds mu, then proceeds after release) and `TestSetEnabled_RunsBeforePostFetchRecheck_DropsCandidate` (asserts the other valid outcome: `SetEnabled(false)` committed before the post-fetch recheck → recheck observes disabled and drops, no add fn call, no queue mutation, no broadcast, no history append). No global queue/playback/voting/auto-queue, frontend, Docker, migration, or REST contract changes. See sprint `021-room-auto-queue-contract-design.md` (Implementation summary (R09f)). |
| **R09g** | Room auto-queue frontend integration | **Closed (2026-07-01, accepted by the Product Owner)** | Fifth narrow slice of the legacy R09c+ bucket (the room auto-queue frontend wiring). Exposes the R09f backend runtime in the existing room frontend without changing backend contracts: two thin frontend API methods (`api.getRoomAutoQueueStatus(slug)`, `api.setRoomAutoQueueEnabled(slug, enabled)` with body exactly `{ enabled }`); an isolated per-room `autoQueueConfig` slice under `globalStore.roomQueues[slug]` (default `{ enabled: false, strategy: 'related' }`) plus three mutators (`setRoomAutoQueueConfig`, `applyRoomAutoQueueAdded` preferring `payload.state`, `applyRoomAutoQueueConfigChanged`); a small `📻 Radio` toggle in RoomView that fetches status on mount + slug change, is disabled when disconnected, unauthenticated, or a toggle is in flight, and maps 401/403/404/409 to clear toasts; and two new WebSocket listeners in RoomView (`room_auto_queue_added`, `room_auto_queue_config_changed`) routed through the per-room mutators. The toggle is a UI convenience only; the backend remains authoritative on host/admin authorization, and the frontend does NOT pre-check the role. The global `/api/autoqueue/...` contract, the global `/ws` 16-event inventory, the global `globalStore.autoQueueConfig` slice, the `frontend/src/views/DashboardView.vue` radio button, and the R09f backend implementation are all unchanged. R09g only touches `frontend/src/services/api.js`, `frontend/src/store/index.js`, `frontend/src/views/RoomView.vue`, and their existing test files; no Go, migration, Docker, or REST/WebSocket contract changes. Verification: `cd frontend && npm run test:unit -- --run` (all pass) and `cd frontend && npm run build` (clean). See sprint `021-room-auto-queue-contract-design.md` (Implementation summary (R09g)). |
| **R10a** | Room deletion and membership removal — contract design | **Accepted (2026-07-02)** | First narrow slice of the legacy R10 bucket. R10a is a **documentation-only** planning/design sprint that shapes a narrow, reviewable contract for a future room deletion / member removal runtime implementation. R10a introduces **no** Go runtime code, **no** Vue runtime code, **no** migrations, **no** routes, **no** WebSocket constants, and **no** config changes. The contract settles: soft-archive semantics for "delete room" (hard delete / hard purge / cleanup is **out of scope**); host role as the owner equivalent (no new role/owner column); future endpoint contracts `DELETE /api/rooms/{slug}` (host-only, idempotent on archived rooms) and `DELETE /api/rooms/{slug}/members/{userId}` (host-only; cannot remove self; cannot remove host; `409` on archived room); confirmation stance (no body in R10b — confirmed-by-bearer-token + host role); HTTP status-code mapping; archived-room behavior for each endpoint; member-removal behavior with immediate loss of room membership; session-token stance (no room-scoped revocation — enforce via membership checks + per-room WS disconnect/notification only); player-lease effects (removed active lease holder has lease ended in the same transaction as the membership delete; room archive ends active lease idempotently via the existing R06 path); room-vote effects (removed user MUST NOT count in future thresholds; active sessions are **left to natural expiry**); per-room WebSocket event contracts (no new event-name constant in R10a — reuses the existing R06 `room_archived` envelope for archive; documents two new envelopes for the future runtime: `room_member_removed` delivered to the removed client before close, and `room_members_changed` delivered to remaining clients); an optional audit table shape for the future runtime; and a future test matrix covering usecase, repository, HTTP handlers, WebSocket hub, and race-sensitive lease/vote/member paths. Single-instance only; no cross-process safety claim. The future R10b runtime implementation is the next slice and remains deferred. See sprint `015-room-deletion-and-membership-removal.md`. |
| **R10b** | Room deletion and membership removal — backend runtime implementation | **Closed (2026-07-02 implemented on `dev`); accepted by the Product Owner (2026-07-10)** | Backend-only runtime implementation of the R10a contract. R10b lands the two HTTP routes (`DELETE /api/rooms/{slug}`, `DELETE /api/rooms/{slug}/members/{userId}`), the membership-delete + lease-end transaction, the three per-room WebSocket envelopes on `/ws/rooms/{slug}` only — `room_member_removed` (delivered to the removed client before close), `room_members_changed` (delivered to remaining clients), and the per-room-hub `room_archived` broadcast (with `reason: "host_archived"`, reusing the existing R06 wire value + payload via new explicit per-room hub support; the global `/ws` archive broadcast continues to ride the R06 path unchanged) — and covers the R10a test matrix. R10b is **backend/runtime only**: no Vue runtime code, no frontend API methods, no frontend store, no RoomView controls, no frontend tests. Frontend UI wiring for delete / remove controls is the **R10c** slice. No hard delete, no retention windows, no per-room session-token revocation, no cross-process safety claim. R10b conforms to the R10a contract in `015-room-deletion-and-membership-removal.md`. |
| **R10c** | Room deletion and membership removal — frontend UI | **Closed (2026-07-10 implemented on `dev`); accepted by the Product Owner (2026-07-10)** | Frontend UI wiring for the R10b backend, with a narrow backend-supported initial-paint read surface so the host remove-member UI has an authoritative source before any `room_members_changed` event fires. Frontend: RoomView delete-room control (host only), the RoomView remove-member control (host only) with confirmation surface, frontend API methods (`api.deleteRoom(slug)`, `api.removeRoomMember(slug, userId)`, `api.getRoomMembers(slug)`), per-room store slice for the new WS envelopes (`archived`, `removed`, `members`), three new WS listeners in RoomView (`room_archived`, `room_member_removed`, `room_members_changed`), and a 1008-close-driven removed-state branch in RoomView's `onClose` (the per-room WS client now passes the `CloseEvent` to `onClose`). Backend delta: `HandleListMembers` response body wrapped to `{"members": [{user_id, role}]}` so the initial REST read routes through the same `applyRoomMembersChanged` mutator as the post-removal WS delta — active-room only, archived maps to 409, any active member may read (the roster is not restricted to host); `joined_at` is intentionally omitted (per R10a Decision 9); no mutation, no broadcast, no client-supplied identity fields. Conforms to the R10a contract and the R10b runtime; explicitly deferred so R10b could ship backend-only. |
| **R10d** | R10c closure polish and docs acceptance | **Closed (2026-07-10 implemented on `dev`); accepted by the Product Owner (2026-07-10)** | Small, narrow cleanup sprint after R10c acceptance. R10d does NOT advance runtime or contract work: it only updates project-management documentation status (marks R10c closed/accepted by the Product Owner on 2026-07-10, moves R10c out of active/current status, sets R10d as the current active sprint at the time; R11 remains planned and is NOT marked active), corrects one narrow frontend empty-state copy in `frontend/src/views/RoomView.vue` so the host member-list no longer says the next `room_members_changed` event will populate the list (R10c now seeds members from `GET /api/rooms/{slug}/members`; new copy is the simple "No members loaded yet."), and adds one focused success-path test for the existing `HandleListMembers` HTTP handler (active room, active actor member; asserts `200`, asserts response body shape is `{"members": [{"user_id", "role"}]}`, and asserts `joined_at` is not present anywhere in the JSON response). R10d preserves the existing 401/403/404/409 behaviour and the existing wire shape; does NOT change R10c runtime behaviour, REST route paths, WebSocket event names or payloads, store mutators, RoomView logic other than the empty-state copy, auth/session behaviour, Docker, migrations, room queue/playback/vote/auto-queue, or R11 (room chat). |
| **R10e** | Roadmap/status refresh after R10d acceptance | **Closed (2026-07-10 implemented on `dev`); accepted by the Product Owner (2026-07-10)** | Small, narrow docs-and-tooling sprint after R10d acceptance. R10e does NOT advance runtime or contract work: it only updates `documents/00-project-management/PROJECT_STATE.md` (current-state summary section + baseline date), `documents/00-project-management/SPRINTS/active.md` (R10d closed/accepted; R10e set as current/active; R11 stays Planned and is NOT marked active), and `documents/00-project-management/ROOM_EPIC_SPRINT_SEQUENCE.md` (this row); plus it renames the previously-conflicting `R10d+` future bucket to `R10f+ — Deferred room lifecycle hardening` (preserving its deferred scope: hard delete/retention, admin audit read surface, per-room session revocation, archived-room cleanup; explicitly NOT required before R11 unless the Product Owner decides otherwise); and it adds minimal frontend tooling metadata so contributors do not run frontend verification on Node 18 by mistake — an `engines.node` field in `frontend/package.json` matching the current frontend dependency baseline `^20.19.0 || ^22.12.0 || >=24.0.0`, and `frontend/.nvmrc` pinned to Node 22. R10e does NOT change dependency versions, dependency lockfile content beyond any lockfile metadata naturally produced by the `engines` field, REST route paths, WebSocket event names or payloads, store mutators, runtime code, migrations, Docker/Nginx/deployment, or R11 (room chat). |
| **R11a** | Room chat contract and minimal vertical slice | **Closed (2026-07-15 implemented on `dev`); accepted by the Product Owner (2026-07-15)** | First narrow, full-stack vertical slice of the legacy R11 room-chat bucket. Backend: a new `RoomChatMessage` entity + `MaxChatContentLen = 500` + `NormalizeChatContent` (trim, CRLF/CR→LF, reject empty, reject over-cap); a new `RoomChatMessageRepository` interface; a new `PostgresRoomChatMessageRepository` over migration `0008_room_chat_messages` (`room_chat_messages` table with `(room_id, created_at)` index and FKs to `rooms` + `users`); a new `internal/usecase/roomchat` package with a small set of narrow interactor seams (`ChatMessageRepository`, `RoomLookup`, `UserLookup`, `Broadcaster`) so usecase tests inject fakes; a new `RoomChatHandlers` exposing `GET /api/rooms/{slug}/chat/messages?limit=50` (default 50, max 100; out-of-range limits map to 400) and `POST /api/rooms/{slug}/chat/messages` (body exactly `{"content": "..."}`; sender identity is server-resolved from the bearer token, never from the wire); the additive per-room WebSocket envelope `room_chat_message_created` on `/ws/rooms/{slug}` only (the global `/ws` 16-event inventory is unchanged) with the same wire shape as the REST response so the frontend can route both through the same store mutator; a `roomChatBroadcasterAdapter` in `cmd/server/main.go` satisfying the broadcaster seam; the wire NEVER exposes the sender email; `display_name` falls back to `user #<id>` when the user row's `display_name` is empty. Frontend: two thin API methods (`api.getRoomChatMessages(slug, limit=50)`, `api.sendRoomChatMessage(slug, content)`); an isolated `messages: []` slice on `globalStore.roomQueues[slug]` with `setRoomChatMessages` (REST seed) + `applyRoomChatMessageCreated` (WS append) + `applyRoomChatMessageFromPost` mutators capped at `MaxRoomChatMessages = 100`; a minimal `chat-panel` in `RoomView` (history list rendered as text via `{{ }}`, never `v-html`; input + Send button; history seed AFTER the first per-room `room_queue_sync` event, NOT on mount before WebSocket registration; re-seeded on slug change; `case 'room_chat_message_created'` in `applyMessage`); `canEditChat` / `canSendChat` split (the input stays editable when the draft exceeds 500 Unicode code points so the user can shorten it; the Send button is disabled while over-limit); no native `maxlength` attribute on the input; the `chatDraftCodePoints` computed uses `Array.from(chatDraft).length`. R11a is **intentionally not** the full R11 contract: it ships plain text only, no retention purge, no moderation surface, no audit log, no attachments, no markdown/rich text, no typing indicators, no read receipts, no WebSocket send command, no dedicated chat WebSocket endpoint, and no cross-process guarantees. R11a preserves: the global `/ws` 16-event inventory, the R06 `room_archived` wire value + payload, the R07b/R07d/R09a/R09b/R09c/R09d/R09f/R10b per-room WebSocket events, the R05 in-memory session-token model, the R10a members-list response wrapper, the global room-queue + auto-queue + vote + playback + member-removal surfaces, the R10f+ deferred lifecycle hardening bucket, and the R12/R13/R14 planned sprints. The schema version moves to 8 (after migration 0008); the per-room WebSocket event inventory gains the additive `room_chat_message_created` envelope. R11a is closed and accepted by the Product Owner (2026-07-15); R11b+ (deferred R11 slices — moderation, edit/delete, audit, retention, attachments, typing indicators, read receipts, WebSocket send command, dedicated chat WS endpoint, richer sender profile) remains planned and is NOT pulled forward. The R10a/R10b/R10c/R10d/R10e sprint records are unchanged by R11a. Verification: `go test -count=1 ./internal/usecase/roomchat/... ./internal/delivery/http/... ./internal/delivery/ws/... ./internal/infrastructure/persistence/... ./cmd/server/...`, `go test -race -count=1 ./internal/delivery/ws/... ./internal/usecase/roomchat/...`, `cd frontend && npm run test:unit -- --run` (305/305), `cd frontend && npm run build` (clean), `git diff --check` (clean). See [`SPRINTS/016-room-chat-feature.md`](./SPRINTS/016-room-chat-feature.md) for the full R11a contract + the deferred R11b+ scope + the closure notes. |
| **R10f+** | Deferred room lifecycle hardening | **Planned (deferred; NOT required before R11 unless the Product Owner decides otherwise)** | Post-R10 deferred lifecycle hardening bucket. Was previously labelled `R10d+`; renamed to `R10f+` during R10e to resolve the R10d / R10d+ naming collision now that R10d is an accepted sprint. Preserves the originally-deferred scope: hard delete of archived rooms, retention windows for the soft-archive state, an admin audit-log read endpoint, and per-room session-token revocation, plus archived-room cleanup. None of these are part of R10a/R10b/R10c/R10d; none block R11. |
| **R10** | Room deletion and membership removal (legacy stub) | **Superseded by R10a + R10b** | The original R10 stub is now split into R10a (contract design) + R10b (runtime). This row is retained as a high-level reference only; the contract lives in `015-room-deletion-and-membership-removal.md` under R10a and the runtime lands under R10b. |
| **R11** | Room chat feature | **Planned (split: R11a closed/accepted 2026-07-15; R11b+ planned and NOT active)** | Introduce a lightweight chat system within each room. Persist chat messages, broadcast them via WebSocket and enforce simple moderation rules. The R11 bucket is now split into R11a (room chat contract and minimal vertical slice — narrow, full-stack, plain-text only — closed and accepted by the Product Owner on 2026-07-15) and R11b+ (deferred R11 scope: message edit/delete, moderation, audit log, retention purge, attachments, markdown/rich text, typing indicators, read receipts, WebSocket send command, dedicated chat WS endpoint, offline push, cross-room/global chat, cross-process guarantees, richer sender profile data). R11b+ remains planned and is NOT pulled forward by the Product Owner; nothing is currently active. See [`SPRINTS/016-room-chat-feature.md`](./SPRINTS/016-room-chat-feature.md). |
| **R12** | Search and room discovery | **Planned** | Add search endpoints to find available tracks (via external providers) and discover public rooms. Implement pagination, filtering and result caching. See sprint `017-search-and-room-discovery.md`. |
| **R13** | Authentication and authorization hardening | **Planned** | Strengthen the authentication subsystem with refresh tokens or JWT, refine role‑based authorization and lock down all endpoints. See sprint `018-authentication-and-authorization-hardening.md`. |
| **R14** | Global contract cleanup and documentation | **Planned (split: R14a active 2026-07-15; R05b / R09h / R14b / R14c / R14d / R14e planned and NOT active; revised 2026-07-15 after Architect review)** | Perform a final pass over all contracts, code and documentation. Remove deprecated behaviours, update ADRs, publish OpenAPI definitions and ensure test coverage. The legacy R14 stub is split into R14a (active) + R05b + R09h + R14b + R14c + R14d + R14e. R14a — Legacy global state migration and contract retirement plan — is the **current active planning/design sprint on `dev` (2026-07-15)**, documentation only. R14a introduces no Go runtime code, no Vue runtime code, no migrations, no routes, no WebSocket constants, no Docker / Nginx / environment / dependency / deployment changes, and no schema version bump (the schema stays v8 throughout R14a–R14d and the rollback window; v9 lands ONLY if R14e accepts destructive cleanup; historical migration files `0001`..`0008` remain on disk). R14a produces an implementation-ready contract for converting the legacy global state (`queue_state`, `activities`, `auto_queue_config`, `play_history`) into exactly one Product-Owner-named room and for retiring the parallel global REST/WS contracts in controlled later slices. **Two blocking prerequisites for R14c** are recorded — R05b (room entry / player-lease UI, blocks user-facing cutover) and R09h (vote-to-prioritize parity, blocks retirement of `/api/vote/prioritize` only). The cutover sequence is split — R14b builds and verifies the offline mechanism (a NEW dedicated `cmd/room-cutover` CLI with NEW dedicated `room_cutover_marker` contract; R03's `cmd/migrate-data` + `migration_marker` are preserved untouched); R14c owns the coordinated production execution with a binary built `--room-cutover-authoritative=true` that unregisters the legacy global REST routes and the global `/ws` endpoint. The new ADR `documents/00-project-management/ADRS/003-legacy-global-state-migration-and-contract-retirement.md` is **Proposed** — pending Architect review and Product Owner acceptance; it supersedes parts of ADR 001 (and the R06-fold attribution in ADR 002 §11/§13) without rewriting them. ADR 001 body §3–§18 is unchanged byte-for-byte except for one metadata supersession note near the header; ADR 002 body is unchanged. R13 (session / authentication / authorization hardening), R10f+ (deferred room lifecycle hardening), R11b+ (deferred R11 chat slice), and R12 (search and room discovery) are NOT pulled into R14a scope. See sprint [`022-legacy-global-state-migration-and-contract-retirement-plan.md`](./SPRINTS/022-legacy-global-state-migration-and-contract-retirement-plan.md) and the legacy stub [`019-global-contract-cleanup-and-documentation.md`](./SPRINTS/019-global-contract-cleanup-and-documentation.md). |
| **R05b** | Room entry / player-lease UI | **Planned (blocking prerequisite for R14c — NOT active; NOT pulled forward unless the Product Owner decides otherwise)** | Frontend-only sprint that wires the SPA to the existing R04 / R06 / R11a backend routes. Adds the SPA client methods `api.createRoom` / `api.listRooms` / `api.getRoom` / `api.joinRoom` / `api.redeemInvite` / `api.claimPlayerLease` / `api.heartbeatPlayerLease` / `api.releasePlayerLease` / `api.getPlayerLease`; exposes them as actual room create / list / join / invite-redeem / lease-claim UI surfaces. Today the backend routes are registered (`cmd/server/main.go:413–486`) but no SPA client methods or UI exist; rooms are entered only by direct URL (`/rooms/:slug`). **R05b is a BLOCKING PREREQUISITE for R14c** — without it, retiring `/api/queue` / `/ws` etc. strands users on a global dashboard with no way to enter a room. No new HTTP routes, no new server-side code beyond what the existing routes already provide. R05b is separate from R10c (R10c is the host-only delete-room / remove-member controls added in R10c); R05b is the broad room-entry surface. Verification: frontend tests (`cd frontend && npm run test:unit -- --run`, `cd frontend && npm run build`); manual UI walk. Single-instance only; no cross-process safety claim. |
| **R09h** | Room vote-to-prioritize parity | **Planned (blocking prerequisite for R14c — only for `/api/vote/prioritize` retirement; NOT active; NOT pulled forward unless the Product Owner decides otherwise)** | Backend-only parity sprint that closes the `/api/vote/prioritize` retirement gap before R14c. Adds `POST /api/rooms/{slug}/vote/prioritize` with body exactly `{"song_index": <integer>}`; actor identity comes only from the bearer token (never from the body). No priority-balance debit; no priority transaction. Mirrors the R09b session shape: in-memory vote session keyed `prioritize:{slug}:{songID}`, 30-second expiry, single-instance only, threshold `max(2, uniqueConnectedUserIDs(slug) / 2 + 1)` captured at session creation via `(*RoomWSHub).UniqueConnectedUserIDs(slug)` (de-duplicated by `userID` so a user with multiple tabs counts once). Any active room member may vote; sessions are room-scoped and in memory. On a passed vote, a queue-owned `roomqueue.Interactor` operation re-resolves the target by song ID (index as hint only), rejects the current song (sentinel maps to `400`), detects stale removal or queue movement as stale (sentinel maps to `409`), runs under the room queue mutation lock, moves the target immediately after the current song, persists once, and emits `room_vote_updated` + `room_vote_resolved` + the existing `room_queue_song_prioritized` event (NO new WS event constant — the existing R07d event is reused additively). Frontend UI MAY be a separate small follow-up; the backend replacement MUST exist before R14c may UNREGISTER the `/api/vote/prioritize` route. **Distinguish from R07d:** R07d is a *queue mutation* (`/api/rooms/{slug}/queue/prioritize`) with a host/admin actor; R09h is a *vote-driven queue mutation* with member-actor votes. The global `/api/queue/prioritize` already has an R07d room equivalent and retires in R14c with NO parity sprint; only `/api/vote/prioritize` needs R09h. R09h is the ONLY sprint that may legally UNREGISTER `/api/vote/prioritize` in R14c; R14b stays offline-mechanism-only. Single-instance only; no cross-process safety claim. |
| **R14b** | Room-cutover mechanism (backend only) | **Planned (NOT active; NOT pulled forward unless the Product Owner decides otherwise; follows R14a)** | Backend-only sprint that ships the offline cutover mechanism. Lands: a NEW dedicated CLI `cmd/room-cutover` with subcommands `plan` (read-only snapshot), `up` (apply), `verify` (re-assert hash record), and `abort` (release the advisory lock without committing); a NEW dedicated marker table `room_cutover_marker` (single-row, `id = 1`) carrying `room_cutover_id` (UUID), `target_room_slug`, `target_room_id`, `host_user_id`, per-source SHA256 hashes (`queue_state.data`, `activities.*`, `auto_queue_config.*`, `play_history.*`), per-target post-copy SHA256 hashes (`room_queue_state.data` for the migrated room, `room_activities.*` filtered by `room_id`, `room_auto_queue_config.*` filtered by `room_id`, `room_play_history.*` filtered by `room_id`), `legacy_id_offset` (for `room_play_history` id-collision handling — the `room_play_history` BIGSERIAL is shared across all rooms, so legacy rows are shifted to avoid clashing with any room's auto-queue rows since R09f), `cutover_started_at`, `cutover_committed_at`, `binary_build_sha`; the binary build flag `--room-cutover-authoritative` (default `false`). Lands source-to-target copy: a NEW `room_activities` table + dedicated `RoomActivityRepository` (NOT coupled to `QueueRepository`); host bootstrap via `--host-user-id=<positive integer>` (canonical `users.id` BIGINT; PK lookup; no inference from joiners / roles / emails / lease / frontend; per-room one-host invariant, not per-user globally); lossless source-to-target mapping (the default `encoding/json` tagged-field round-trip on `entity.Queue` for `queue_state` → `room_queue_state`, validating all invariants and computing SHA256 over the canonical JSONB bytes; preserved id/timestamp/type/user/description/count for `activities` with `setval(room_activities_id_seq, MAX(id), is_called=true)`; per-room write of `room_auto_queue_config` from the legacy singleton; per-room `play_history` copy with the `legacy_id_offset` shift and `setval(room_play_history_id_seq, MAX(id), is_called=true)`). `users` / `user_sessions` / `priority_transactions` are NOT recopied (account-scoped per ADR 001 §3 Decision 9); the migrator does NOT touch these tables; idempotency is read-only against `users`. **R14b does NOT reuse R03's CLI or `migration_marker`**; it draws on R03 patterns (advisory lock, single transaction, hash record, dry-run, PII redaction) but the CLI and marker are NEW and dedicated. R03's `cmd/migrate-data` + `migration_marker` are preserved untouched. **NO endpoint removal. NO frontend changes.** R14b verification gate (must pass before R14c can run): `cmd/room-cutover plan` against a fresh snapshot returns a PII-free report; `--dry-run` completes with no writes and a successful SHA256 record; real `up` completes the source-to-target copy inside one transaction, commits the marker, returns 0; re-running `up` immediately returns `already cut over; no-op` (exit 0), driven by the marker; hash drift on either source or target is detected and rejected (no `--force` / `--reset`); PII redaction verified; the R03 marker is preserved untouched; `go test ./...` clean. Single-instance only; no cross-process safety claim. See [`./SPRINTS/022-legacy-global-state-migration-and-contract-retirement-plan.md`](./SPRINTS/022-legacy-global-state-migration-and-contract-retirement-plan.md) § Future sprint sequence for the full contract. |
| **R14c** | Coordinated production cutover (backend only) | **Planned (NOT active; NOT pulled forward unless the Product Owner decides otherwise; follows R14b after R14b's verification gate passes; follows R05b for user-facing cutover; follows R09h for `/api/vote/prioritize` retirement)** | Backend-only sprint that owns the **coordinated production execution**. Maintains the deployment compatibility matrix in R14a § Deployment compatibility matrix. Before the window: a `pg_dump` is taken and retained ≥30 days (mirrors ADR 002 §9); R14b's binary is already deployed and R14b's verification gate has passed. During the window: the application is shut down (or restarted with a maintenance-mode flag that disables ALL mutating endpoints — global AND room — while keeping reads disabled too in this state); `cmd/room-cutover up` runs against the live database (advisory lock, single transaction, copy, commit, marker); the application restarts with `--room-cutover-authoritative=true`. In this build: the legacy global `/api/queue/...` / `/api/vote/skip` / `/api/autoqueue/...` routes and the legacy `/ws` endpoint are **unregistered**, NOT just "must not be hit" — the mux returns HTTP 404 for unregistered legacy routes; the room routes are the only registered set; the legacy repositories remain in the binary for read-only rollback support but are NOT wired into HTTP handlers or the WS hub; legacy write paths are disabled. The proposed documentation envelope (NOT carried on the wire — the mux 404 is the actual response) is `{error: "gone", code: "global_contract_retired", documentation: "documents/00-project-management/SPRINTS/022-legacy-global-state-migration-and-contract-retirement-plan.md"}` with no concrete `successor` value — clients discover the migrated room via `GET /api/rooms`; the HTTP `Link: </api/rooms>; rel="successor-version"` header MAY be added if the Product Owner wants explicit discovery. OUT of retirement: `POST /api/auth/google`, `POST /api/auth` (Deprecated), `GET /api/user/priority-balance`, `GET /api/youtube/search`. **Blocked on R09h:** `/api/vote/prioritize` is explicitly EXCLUDED from the unregistration set until R09h lands AND is accepted by the Product Owner; R14c may UNREGISTER the OTHER legacy routes without R09h. Per-room runtime is the only authoritative path. The 16-event global `/ws` inventory and the per-room WebSocket event inventory (R07b/R07d/R09a/R09b/R09c/R09d/R09f/R10b/R11a) are unchanged; no new WS event constant in R14c. **NO frontend changes in R14c** — frontend cutover is R14d. The post-R14c binary must be paired with the post-R05b + post-R14d frontend per the compatibility matrix; pairing it with a DashboardView-only frontend strands users. Verification: HTTP 404 tests for unregistered routes; `/ws` upgrade rejection; per-room WebSocket event inventory unchanged; success-path routing; deployment compatibility matrix rows (each combination exercised). Single-instance only; no cross-process safety claim. See [`./SPRINTS/022-legacy-global-state-migration-and-contract-retirement-plan.md`](./SPRINTS/022-legacy-global-state-migration-and-contract-retirement-plan.md) § Future sprint sequence and § Deployment compatibility matrix. |
| **R14d** | Frontend global-path removal | **Planned (NOT active; NOT pulled forward unless the Product Owner decides otherwise; follows R14c; assumes R05b has landed)** | Frontend-only sprint that implements Phase C. `RoomView` becomes the only supported view; `DashboardView` is removed or redirects to the room entry surface from R05b. `globalStore.queueState`, `globalStore.voteSessions`, and `globalStore.autoQueueConfig` are cleared. `globalStore.currentUser` is preserved (RoomView reads it). `globalStore.roomQueues[slug]` per-slug slices are preserved (RoomView reads them). The global `WebSocketClient` is closed and not reconnected. Stale bookmarks, stale local storage, archived-room view, and HTTP 404 responses from unregistered legacy routes surface as clear UI states; the frontend MUST NOT attempt to fall back to the unregistered endpoints. **R14d is paired with the post-R14c binary at production cutover time per the compatibility matrix.** R14d only touches `frontend/src/services/api.js`, `frontend/src/services/websocket.js`, `frontend/src/store/index.js`, `frontend/src/router/index.js`, `frontend/src/views/DashboardView.vue`, `frontend/src/views/RoomView.vue`, and their existing test files; no Go, migration, Docker, or backend REST/WebSocket contract changes. Backend contracts are fixed by R14c. Verification: frontend test suite (`cd frontend && npm run test:unit -- --run`, `cd frontend && npm run build`); manual compatibility-matrix walk. Single-instance only; no cross-process safety claim. See [`./SPRINTS/022-legacy-global-state-migration-and-contract-retirement-plan.md`](./SPRINTS/022-legacy-global-state-migration-and-contract-retirement-plan.md) § Frontend transition & recovery and § Deployment compatibility matrix. |
| **R14e** | Schema cleanup (backend only) | **Planned (NOT active; NOT pulled forward unless the Product Owner decides otherwise; follows R14d; destructive cleanup only after the verified rollback window AND after no runtime path references the legacy tables)** | Backend-only destructive sprint that implements Phase D. Drops the legacy `queue_state`, `activities`, `auto_queue_config`, and `play_history` tables. Reduces `0001_initial.up.sql` to only the account-scoped tables (`users`, `user_sessions`, `priority_transactions`); the historical migration files (`0001`..`0008`) remain on disk so the v8 era remains auditable. Schema version bumps from 8 to 9 (only if R14e accepts and destructive cleanup completes). The legacy `cmd/migrate-data` (R03) is preserved untouched; it may be retained for rollback evidence but its global-state migration paths are no longer needed once the legacy tables are gone. NO new functionality in R14e. Verification: focused cleanup tests confirm no runtime path references the legacy tables; `go test ./...` clean; frontend tests clean; `git diff --check` clean. Rollback: if a rollback is required after R14e, recovery requires forward reconciliation (legacy data is gone); this is documented in R14a § Deployment, backup, and rollback gates. Single-instance only; no cross-process safety claim. |

## How to use this document

* When shaping a new sprint, refer to the stub file listed in the **Description** column.  Each stub provides a goal, describes the baseline behaviour, outlines desired changes, lists the affected context and gives implementation guidance.
* Once a sprint completes, update the **Status** in the table to **Closed** and summarise the outcome.  If any decisions change the scope of later sprints, reflect that here.
* If you add a new sprint, append it to the bottom of the table with a short description and create a matching stub file under `documents/00-project-management/SPRINTS`.
